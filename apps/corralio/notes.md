# Corralio Notes

## 2026-09-02 — Gate 3 isolated Auth/runtime path verified end to end with mock provider

- Completed the separately authorized isolated Supabase Auth/runtime verification and filed the durable CPO closeout at `docs/corralio/cpo/2026-09-02-cpo-report-gate3-isolated-auth-runtime-verification.md`. The final verified chain is: real Turnstile → Corralio durable request authorization → one-use phone permit → `signInWithOtp()` → genuinely signed Supabase Send SMS Hook → atomic hook authorization/segment reservation → exactly one mock-provider invocation → successful Supabase Auth request completion (`HTTP 202`). No Telnyx credential, Telnyx request, handset delivery, production configuration, or Phase A+B product implementation participated.
- Added a closed, privacy-safe Send SMS Hook pre-authorization diagnostic vocabulary covering unavailable secret, invalid headers/timestamp, signature mismatch, invalid JSON/payload shape, invalid phone, invalid OTP, and unknown pre-authorization failure. These categories are returned only internally for bounded operational diagnosis; raw phone, OTP, payload, signature, Turnstile token, credentials, and arbitrary provider/Auth text remain excluded. Deterministic tests prove all pre-authorization failures stop before database authorization and provider work.
- Corrected the hosted Supabase HTTP Hook success contract established by the isolated run. A successful response is now `HTTP 200`, `Content-Type: application/json`, body `{}`. The first bounded retest proved the saved hook-secret pairing issue (`signature_mismatch`); the second proved the missing JSON content type (`hook_payload_invalid_content_type`); the third proved that the hosted Auth implementation rejects an empty JSON response body with a generic failure; and the fourth completed successfully with the minimal JSON object. One intervening request was correctly rejected by the durable hourly rate limit before Supabase Auth and consumed no Auth call, permit, hook attempt, segment, or mock invocation.
- Exact authorized-session accounting: maximum additional `signInWithOtp()` calls `5`, actual calls `4`, unused calls `1`, signed hook deliveries `4`, authorized segment reservations `3`, mock-provider invocations `3`, hook retries/duplicates `0`, Telnyx attempts `0`, handset deliveries `0`. The three reservations were intentionally permanent during the test session, consistent with the Gate 3 at-most-one provider-attempt invariant.
- Cleanup passed. The disposable Auth identity was removed; all synthetic rows were removed across all eight durable-state tables; the original durable policy, including `updated_at`, was restored exactly; the Send SMS Hook was deleted; isolated CAPTCHA and Phone Auth were disabled; temporary Vercel runtime secrets and the enable flag were removed; and the retained page/API routes now fail closed with `HTTP 404`. The Cloudflare widget and repository test surface remain retained for future separately authorized testing. Temporary helpers were deleted and the clipboard cleared. A temporary hook secret appeared in browser-automation output during cleanup; deleting the hook and removing the paired Vercel secret invalidated it immediately.
- Verification passed 24 focused tests, explicit Corralio TypeScript, zero-warning Corralio lint, the isolated Corralio production build, and `git diff --check`. Verdict: **GATE 3 ISOLATED AUTH/RUNTIME VERIFICATION PASSED — MOCK PROVIDER ONLY**. This closes the Supabase/Turnstile/signed-hook/mock-runtime sub-gate; it does not authorize Telnyx, handset OTP, production configuration, Phase A+B implementation, push, or deployment.

## 2026-09-02 — Isolated Turnstile/Supabase pairing corrected and verified without Auth

- Filed and executed the bounded correction contract at `docs/prompts/corralio-gate-3-turnstile-secret-pairing-correction-prompt.md`. Reconfirmed isolated Supabase project `azuwuouctkyppkrugnls`, the unique managed Cloudflare widget `corralio-gate3-isolated`, its exact sole hostname `corralio-gate3-isolated.vercel.app`, and the retained deployment. The deployed page returned HTTP `200`, contained exactly one detected Turnstile site-key candidate, and that candidate matched the retained widget.
- Corrected an evidence error from the preceding audit: the Supabase Management API returns a redacted CAPTCHA-secret representation, so hashing that response cannot prove a secret mismatch. Under the founder's bounded authorization, the current widget secret was read from the Cloudflare widget-detail endpoint, fingerprinted without disclosure, transferred only through the local clipboard, entered directly into the isolated Supabase CAPTCHA field, and saved successfully. The controlled source-to-field path establishes the pairing; no repository file, command argument, screenshot, snapshot, log, or report contained the secret.
- Read-only post-save configuration confirmed HTTP `200`, provider `turnstile`, a present redacted secret, and CAPTCHA returned to disabled. The isolated Vercel runtime still contains only `CORRALIO_GATE3_ISOLATED_RUNTIME`, `CORRALIO_SITE_URL`, and `NEXT_PUBLIC_CORRALIO_GATE3_TURNSTILE_SITE_KEY`; no `TELNYX_*` credential is present. The secure clipboard and temporary audit credentials/files were cleared after use.
- Browser/source verification confirms one fresh Turnstile token is claimed once and passed to the single `signInWithOtp()` call site; Corralio has no separate Siteverify redemption. Privacy-safe diagnostics distinguish the bounded categories `missing_token`, `expired_or_reused_token`, `wrong_secret_sitekey_pairing`, `hostname_or_configuration_mismatch`, and `generic_captcha_failed` without retaining secrets or arbitrary provider text.
- Verification passed 31 focused SMS/Turnstile tests, explicit Corralio TypeScript, zero-warning lint, and `git diff --check`. Exact external-call accounting for this correction: `signInWithOtp()` calls `0`, Telnyx attempts `0`, handset deliveries `0`. Verdict: **TURNSTILE/SUPABASE CONTRACT VERIFIED — READY FOR ONE AUTH CALL**. This does not authorize an Auth call; the next isolated call still requires separate founder authorization and recreation of the minimum mock-only runtime configuration.

## 2026-09-01 — Isolated public-key preflight passed; one-call diagnostic stopped pre-hook at CAPTCHA

- Added a fail-closed isolated Supabase public-key preflight. It accepts only a present, whitespace-free printable-ASCII legacy anon JWT whose `iss`, `role`, and project-reference claims match the isolated project and whose project reference is not in the forbidden production/staging set. The actual isolated dashboard key passed without being printed, logged, persisted in repository files, or included in reports. Focused verification passed 23 tests plus Corralio TypeScript, zero-warning lint, and `git diff --check`.
- Used exactly the one newly authorized `signInWithOtp()` call after a real managed Turnstile widget reported success and the durable request boundary authorized the request. Supabase Auth transport began and returned HTTP `400`, typed code `captcha_failed`, class `AuthApiError`, sanitized category `captcha`, a bounded opaque request ID, and 125 ms transport duration. This was a **Supabase pre-hook failure**: Send SMS Hook deliveries `0`, hook retries `0`, mock-provider invocations `0`, Telnyx attempts `0`, handset deliveries `0`, and reserved segments `0`. It therefore did not reproduce or explain the earlier post-hook failure.
- Cleanup restored the exact durable policy snapshot, removed the disposable allowlist/rate/decision/permit rows and Auth user, and independently confirmed the policy table has its one disabled baseline row while the other seven durable-state tables have zero rows: **DATABASE CLEANUP ZERO**. Phone Auth and CAPTCHA are disabled and the Send SMS Hook is deleted.
- Per subsequent founder direction, the isolated Vercel page and domain-restricted Cloudflare widget were retained for future testing instead of deleted. All server credentials and mock-provider authorization were removed from the retained Vercel runtime and it was rebuilt from the Corralio workspace; the page still renders, while the OTP endpoint now fails closed with bounded HTTP `404`. The ignored local Gate 3 Cloudflare credential file is retained for future bounded setup and remains uncommitted. No production configuration, Telnyx call, handset SMS, commit, or push occurred.
- Exact accounting: authorized `signInWithOtp()` calls `1`, actual calls `1`, Supabase Auth HTTP requests `1`, Send SMS Hook deliveries `0`, mock-provider invocations `0`, Telnyx attempts `0`, handset deliveries `0`. Verdict: **SUPABASE POST-HOOK ERROR STILL UNRESOLVED**. A future run must diagnose why Supabase rejected a real Turnstile token before the hook; no additional Auth call is authorized.

## 2026-09-01 — One-call Gate 3 Supabase error diagnostic did not reproduce the post-hook failure

- Executed the separately authorized ceiling of exactly one additional isolated `signInWithOtp()` invocation. Real managed Turnstile completed and the durable request boundary authorized one request, created two HMAC-only rate rows, and issued one open permit. The Auth transport never began: Node rejected a malformed isolated anonymous-key header containing a non-ByteString character before an HTTP request could be sent. The bounded Auth diagnostic was therefore HTTP status `null`, code `null`, class `UnknownAuthError`, category `unknown_auth_error`, request ID `null`, and duration `null`; the Corralio request returned bounded HTTP `400`/`denied`.
- This identifies the cause of this diagnostic call's denial but does **not** identify or reproduce the earlier post-hook Supabase error. No signed Send SMS Hook was delivered, no retry occurred, the mock provider ran zero times, no segment budget row was created, and reserved segments remained zero. Exact accounting: `signInWithOtp()` invocations `1/1`, real Turnstile interactions `1`, Send SMS Hook deliveries `0`, mock-provider invocations `0`, Telnyx attempts `0`, and handset deliveries `0`.
- The smallest correction for any separately authorized follow-up is to provision the isolated anonymous/public Auth key from a validated ASCII credential value and verify only its shape/project identity before deployment. No second call is authorized by this run. The temporary diagnostic runtime was not changed to guess at the original error.
- Restoration passed: isolated Phone Auth and CAPTCHA were disabled, the Send SMS Hook was deleted, the temporary Cloudflare widget and Vercel project/deployments were deleted, the disposable Auth user was removed, and temporary credentials/files were removed. The durable policy was restored byte-for-byte at the field level including `updated_at`; the policy table has its one baseline row and the other seven tables have zero rows. **DATABASE CLEANUP ZERO** and **EXTERNAL ISOLATED CONFIGURATION RESTORED/DISABLED**. Verdict: **SUPABASE POST-HOOK ERROR STILL UNRESOLVED**. No SMS, OTP delivery, Telnyx call, production change, commit, or push occurred.

## 2026-09-01 — Gate 3 Supabase post-hook diagnostic prepared offline

- Compared the isolated Send SMS Hook implementation with current official Supabase documentation. The implemented successful response is HTTP `200`, empty body, no `Content-Type`, and one bounded mock-count header. Supabase explicitly documents an empty HTTP `200` as successful, so response shape is not an identified mismatch and was not changed. Official documentation also fixes the HTTP-hook budget at five seconds and identifies typed Auth codes including `hook_timeout`, `hook_timeout_after_retry`, `sms_send_failed`, `captcha_failed`, `phone_provider_disabled`, `user_not_found`, and `unexpected_failure`.
- Added Stage 2-only, privacy-safe diagnostics. The Auth boundary records only validated HTTP status, bounded Supabase error code/class/category, an allowlisted opaque request ID if supplied, and duration. The hook records only response status, content-type absence, zero response bytes, duration, durable duplicate/retry observation, and mock invocation count. It never records the raw error message, phone, OTP, Turnstile token, hook payload/signature, credentials, cookies, or session data; the browser still receives only `pending` or `denied`.
- Deterministic verification passed 21 focused tests plus Corralio TypeScript, zero-warning lint, and `git diff --check`. Tests prove the exact documented empty-200 response, error sanitization, opaque-ID bounds, diagnostic field bounds, and existing signature/authorization/provider ordering. Minimum follow-up budget requested: **one** additional isolated `signInWithOtp()` call, stopping immediately after its bounded Auth and hook diagnostics are captured. No Auth call, SMS/OTP delivery, Telnyx request, external configuration, deployment, database mutation, commit, or push occurred.
- Verdict: **GATE 3 SUPABASE ERROR DIAGNOSTIC READY FOR AUTH CALL AUTHORIZATION**. The existing Gate 3 blocked verdict remains until a separately authorized isolated call identifies the exact category and succeeds or yields a concrete correction.

## 2026-09-01 — Gate 3 isolated Auth/runtime reached the mock sink; response boundary blocked

- Applied `20260831_corralio_sms_durable_safety_state.sql` atomically to the founder-created isolated Supabase project, which was independently confirmed distinct from the production project. The catalog verifier passed, the rollback-only behavioral verifier passed with cleanup zero, and the corrected real-multi-session verifier passed all four independent races: same webhook, global 19/20 cap, destination cap, and one permit/two webhook IDs. Exact durable-policy restoration and cleanup zero across all eight tables passed. Raw `psql` did not create or update a Supabase migration-ledger table; no ledger manipulation occurred.
- Added the fail-closed isolated runtime implementation required for the next verification step: an isolated same-origin OTP-request route, signed Send SMS Hook route, mock-only provider sink, real GSM-7 segment calculation, and hidden real-Turnstile test surface. The runtime rejects any `TELNYX_*` credential, direct database URL, mismatched Supabase reference, absent isolated/mock flags, and missing secrets. It reuses the existing HMAC domains and service-role RPC gateway. Focused tests passed 12/12, explicit TypeScript and zero-warning lint passed, `git diff --check` passed, and the Corralio production build passed.
- Completed the bounded isolated configuration run after the founder created Cloudflare access. A domain-restricted real managed Turnstile widget, a dedicated mock-only Vercel project, isolated Supabase Phone Auth, and a signed Send SMS Hook were configured temporarily. The runtime contained only the 11 approved isolated variables, no database URL, and zero `TELNYX_*` credentials. Dashboard inspection confirmed all built-in Twilio credential/sender fields were empty and disabled behind the hook. Project identity was distinct from production, and raw `psql` still had not updated a Supabase migration ledger.
- Used the full authorized ceiling of five `signInWithOtp()` calls: missing CAPTCHA and invalid CAPTCHA failed without a session; the official test-key direct-Supabase call reached the signed hook and was denied `missing_permit`; one normal-browser real-widget request created a durable request authorization and permit, reached the genuinely signed hook, atomically authorized one segment, and invoked the mock provider exactly once; one same-origin invalid-CAPTCHA request with a spoofed forwarding header created no hook claim and proved the platform-controlled IP bucket was unchanged by the spoof. Exact accounting: Auth calls `5/5`, signed hook deliveries `2`, Turnstile test-key interactions `1`, real-widget interactions `1`, mock-provider invocations `1`, Telnyx attempts `0`, handset deliveries `0`.
- The real-widget browser request nevertheless surfaced the bounded `denied` result because Supabase returned an error after the hook had already completed its one authorized mock attempt. The durable chain and at-most-one economic boundary are proven, but a successful Auth request response is not. The five-call ceiling prevents another diagnostic Auth request in this run. Deterministic/offline verification passed 36 focused tests covering email-auth non-regression, origin/IP validation, rate/cooldown/permit paths, Standard Webhooks signature/replay/expiry/failure behavior, GSM-7 one-segment enforcement, and no-retry ambiguity.
- Restoration passed. Isolated CAPTCHA and Phone Auth were disabled, the Send SMS Hook was deleted, the temporary Cloudflare widget and temporary Vercel project/deployments were deleted, and the disposable Auth user was removed. The complete durable policy was restored exactly, including `updated_at`; the policy table contains its one baseline row and all other seven durable-state tables contain zero rows. **DATABASE CLEANUP ZERO** and **EXTERNAL ISOLATED CONFIGURATION RESTORED/DISABLED**. Current verdict: **GATE 3 ISOLATED AUTH/RUNTIME CONFIGURATION BLOCKED** pending a bounded, privacy-safe diagnostic of the Supabase post-hook error and separately authorized follow-up Auth call. No live SMS, OTP delivery, Telnyx call, production mutation, commit, push, or production deployment occurred.

## 2026-08-31 — Gate 3 durable distributed SMS safety ready for database verification

- Implemented the repository-only centralized PostgreSQL safety boundary required before isolated phone-OTP configuration. The disabled-by-default durable policy is fixed to `test_allowlist`, 20 global UTC-day segments, 5/destination/day, and 1/message. It also carries bounded 3/destination/hour, 5/IP/hour, 60-second cooldown, and 180-second permit TTL controls. Destination and trusted Vercel client-IP identities use separate server-side HMAC domains; PostgreSQL contains no secret, raw phone/IP, OTP, Turnstile token, message body, credential, or provider payload.
- Added one atomic service-role request RPC that checks policy/allowlist/rate/cooldown and issues one opaque short-lived destination permit. Added one atomic service-role hook RPC that claims the signed webhook ID, consumes one matching permit, rechecks policy/allowlist/segment limits, locks both budgets, permanently reserves one segment, and records provider-attempt authorization before returning `authorized`. Duplicate hooks, direct Supabase calls without a permit, expired permits, denial paths, database failure, provider rejection, timeout, and restart cannot authorize a second attempt or release a segment.
- Added the internal same-origin request and verified Standard Webhooks service seams plus an injected provider interface. The deployed seam accepts `x-vercel-forwarded-for` only in the Vercel runtime, fails closed elsewhere, returns bounded browser/hook decisions, and has no Telnyx implementation or file-ledger fallback. Supabase remains authoritative for Turnstile redemption, OTP creation/verification, failed attempts, expiry, invalidation, and replay.
- Prepared unapplied migration `20260831_corralio_sms_durable_safety_state.sql`, a read-only catalog verifier, a network-free rollback-only behavioral verifier, and a separately guarded real-multi-session PostgreSQL race verifier for same-webhook, 19/20 global-cap, destination-cap, and one-permit/two-webhook contention. The concurrency runner requires an explicit isolated-database confirmation, uses HMAC-only fixtures, restores the prior policy state, and independently asserts cleanup zero. None of the database artifacts were run because the migration is intentionally unapplied.
- Verification passed all 13 focused durable-safety tests, all 358 Corralio/shared tests, explicit Corralio TypeScript, zero-warning Corralio lint, the Corralio production build, concurrency-runner syntax validation, and `git diff --check`. All providers were mocked or absent; no live SMS/OTP, external call, database mutation, provider/configuration change, deployment, commit, or push occurred.
- Verdict: **DURABLE GATE 3 STATE READY FOR DATABASE VERIFICATION**. This does not authorize Track A, isolated-project configuration, a public hook, or a live OTP. A human must apply the migration only to the authorized isolated database, then run the catalog, rollback behavior, and real concurrent-session verifiers before any later configuration gate.

## 2026-08-31 — Gate 3 Supabase Phone Auth / Turnstile / Send SMS Hook offline spike blocked before live OTP

- Executed only the repository, current-contract, and offline/mock portions of the remaining Phase A+B Gate 3 spike and filed `docs/corralio/cpo/2026-08-31-cpo-report-gate3-supabase-turnstile-spike.md`. Existing Corralio email magic-link/password/callback behavior was inspected and left unchanged. Installed Supabase Auth supports phone `signInWithOtp`, `verifyOtp(type: "sms")`, and `captchaToken` at a clean separate orchestration seam.
- Confirmed the correct CAPTCHA boundary: Corralio should enforce its durable request controls and pass the one Turnstile token to Supabase, whose native CAPTCHA configuration performs the authoritative redemption. Corralio must not call Cloudflare Siteverify first and then pass the same single-use token to Supabase. Current environment inspection found no Turnstile site key/secret, no Send SMS Hook secret, no management token for configured Auth settings, and no separately identified isolated Supabase project.
- Supabase HTTP Send SMS Hooks use Standard Webhooks raw-body signing (`webhook-id`, timestamp, signature), carry `user.phone` plus `sms.otp`, accept empty successful responses, and may retry `429`/`503` within a five-second total budget. This exposed the critical remaining safety requirement: one centralized durable `webhook-id` claim must be atomic with request limits and the segment reservation. The existing file-backed ledger proves local cross-process atomicity but cannot be treated as shared durable Vercel state, and its random reservation IDs alone cannot prevent a Supabase retry from sending the same OTP twice.
- Added isolated analysis-only fixtures for U.S. test E.164 handling, Turnstile result constraints, test-only per-IP/per-phone/cooldown/verification policies, Supabase Standard Webhook signing/replay/schema validation, enumeration-safe results, and mocked hook-to-ledger provider-failure propagation. Combined verification passed all 13 tests: 7 new Gate 3 fixtures plus the 6 existing Telnyx ledger/signature tests, including restart persistence and the two-process 19/20 race. The mocked ambiguous provider failure retained its reservation.
- TEST-ONLY fixture values are 5 OTP requests/IP/hour, 3/phone/hour, 60-second resend cooldown, 5 verification failures/challenge, and a proposed isolated-project total of 5 OTP/SMS/hour. They are not production policy. Existing SMS safety remains 20 segments/UTC day globally, 5/destination/day, and 1/message.
- Verdict: **GATE 3 BLOCKED**. Before a live OTP, a human must provide/configure a separate isolated Supabase project and Turnstile widget, approve a signed reachable Send SMS Hook plus centralized durable rate/idempotency/segment state, and establish campaign approval or written Telnyx test authorization. Only then may the founder separately authorize one live OTP segment. No live OTP/SMS, Auth/database/provider/configuration mutation, deployment, `/sms` activation, 10DLC submission, commit, or push occurred.

## 2026-08-31 — Controlled Telnyx provider-contract sub-spike partially verified

- Executed only the founder-authorized Telnyx phases 1–4 and filed the durable evidence report at `docs/corralio/cpo/2026-08-31-cpo-report-telnyx-provider-contract-sub-spike.md`. No live inbound/outbound SMS, Telnyx mutation, Supabase/DNS change, campaign submission, deployment, public-route activation, commit, or push occurred.
- Offline safety verification passed 6/6 focused tests: closed configuration and allowlist enforcement, one-segment maximum, persistent global/per-destination limits, lock/corruption fail-closed behavior, ambiguous reservation retention, an atomic two-process 19-of-20 race, and fixture-only Ed25519 timestamp/signature/payload/type/replay rejection. Actual provider segments authorized, reserved, attempted, accepted, finalized, and delivered were all zero.
- Bounded read-only Telnyx API inspection verified authentication, an enabled intended Messaging Profile, one active U.S. two-way A2P SMS number associated with it, and an enabled daily provider cap matching the founder-reported $5 limit. The profile has no webhook URL. START and STOP auto-response operations are present. HELP is configured by founder/dashboard evidence with the intended 122-character response, but is not API-observable through the bounded read used here; that visibility gap is non-blocking, while actual behavior remains unproven until later authorized post-implementation inbound UAT. Do not send `HELP` during the pre-campaign spike because its automatic response could create unauthorized outbound traffic.
- Current 10DLC state remains `UNPROVEN`: the documented brand-list endpoint returned no visible brand for this API context, while the documented campaign-list path returned not found. That does not override the founder-observed dashboard state; dashboard/provider confirmation remains required before any send authorization.
- Verdict: **PARTIALLY VERIFIED**. The adapter, signed webhook, replay/idempotency, delivery-status, and STOP/START responsibilities are documented well enough for later design, but no configured-account send, delivery, inbound event, or signature is `OBSERVED LIVE`. Full canonical Gate 3 still requires isolated Supabase phone Auth, Send SMS Hook, Cloudflare Turnstile, rate-limit/OTP-abuse verification, and explicitly authorized end-to-end OTP delivery before Track A implementation can be authorized.
- Final Telnyx-only closeout confirms the persistent atomic ledger passed restart persistence and the two-process 19/20 race; API authentication, intended profile, associated messaging-capable number, parseable Ed25519 key, and API-observable enabled $5/day cap are established. Outbound and inbound/delivery contracts are sufficient for later implementation. Remaining Telnyx-specific gates are 10DLC/pre-campaign authorization, future human webhook configuration after a handler exists, and authorized configured-account webhook/keyword agreement UAT. HELP API visibility is not a blocker. Phases 5–6 were not executed and all live-SMS counters remain zero.

## 2026-08-31 — 10DLC legal and public consent surfaces implemented locally

- Accepted the founder-confirmed Telnyx/TCR brand classification: CO Services is registered as a Sole Proprietor and Corralio remains the consumer-facing messaging brand. Official Telnyx guidance distinguishes that campaign classification from message content, so the prepared packet accurately lists the intended OTP/2FA, customer-care, and transactional account/schedule traffic instead of incorrectly describing all traffic as Account Notification.
- Added public `/privacy` and `/terms` Server Component routes with route metadata, semantic/mobile legal layout, CO Services operator identification, adult-provided family/child data treatment, operational-processor disclosures, purpose-based retention language, channel choices, and a complete non-marketing Corralio SMS section. The terms intentionally do not invent governing law, arbitration, venue, or class-action provisions; disclaimer/liability language remains subject to founder/legal review.
- Added a global restrained footer stating `Corralio is a service of CO Services.` with public Privacy and Terms links. Repository inspection found no conflicting Corralio customer-facing SIVAD, TournamentInsights, or RefereeInsights operator claim requiring a broader rewrite.
- Added `/sms` and a reusable exact-keyword opt-in disclosure. It is fail-closed behind server-read `CORRALIO_SMS_OPT_IN_ENABLED=true`; until enabled, the route shows no phone number or live texting CTA and truthfully says SMS access is not available. This prevents users from entering a workflow that the Phase A+B execution gate has not yet allowed the repository to build.
- Filed `docs/corralio/cpo/2026-08-31-corralio-10dlc-campaign-submission-packet.md` with exact campaign description, message flow, START/STOP/HELP copy, privacy-minimized GSM-7 samples (including OTP and a full Corralio link), URLs, campaign attributes, Sole Proprietor classification, founder/legal review items, and the remaining activation/submission gates.
- The existing Phase A+B gate remains authoritative. Sole Proprietor classification resolves the entity/campaign field but does not supply the missing Telnyx test sender/profile/key/public key, CAPTCHA, channel-HMAC key, isolated Supabase test setup, hard SMS cap, or disposable test-phone contract. No SMS adapter, webhook, consent schema, database migration, provider configuration, live send, campaign submission, push, or deployment was created or performed.
- Verification passed 4 focused SMS/legal tests, all 308 Corralio/shared-schedule tests, explicit Corralio TypeScript, zero-warning Corralio lint, React/Next review, `git diff --check`, and production builds for all four workspaces (`corp-app`, `corralio-app`, `referee-app`, and `ti-web`). RI/TI emitted only their existing unrelated warnings. Local browser UAT proved `/privacy` and `/terms` render without authentication; the disabled `/sms` state exposes neither the number nor an SMS link; and the explicitly enabled 375×812 state shows the complete disclosure, full unbroken number, SMS/Terms/Privacy links, no horizontal overflow, and no framework overlay. No provider, database, Auth, analytics, or quota call occurred. Nothing was pushed or deployed.

## 2026-08-31 — Phase A+B execution audit stopped at vendor-spike gate

- Converted the blocker list into the durable founder setup/environment/dashboard/safety contract at `docs/corralio/cpo/2026-08-31-cpo-audit-sms-track-a-test-environment-unblock.md` and filed the gated Telnyx-only follow-up at `docs/prompts/corralio-sms-track-a-controlled-vendor-spike-prompt.md`. The audit separates the Telnyx provider-contract sub-spike from full canonical Gate 3 and post-Stage-1 durable START/STOP/send-suppression UAT, establishes test-only limits of 20 segments/day total, 5/day per allowlisted destination, and 1/message, and keeps outbound/OTP blocked absent campaign approval or explicit written Telnyx test authorization plus an individual founder send authorization. Founder decision: Cloudflare Turnstile is the approved Stage 1 phone-auth CAPTCHA provider, but it remains unconfigured and insufficient by itself as a cost/abuse boundary. No SMS, provider/configuration mutation, tunnel, deployment, public opt-in activation, or campaign submission occurred.
- Reconciled the founder-approved Phase A+B prompt with the repository, local configuration, and the Corralio Vercel project before writing product code. Slice 3.6B Phase 1 is complete at `34d83cf4`; its shared `ics_explicit → source_preference → team_preference → corralio_default` resolver, nullable source preference, and narrow writer remain authoritative and unchanged.
- Official provider-contract review confirmed that Supabase supports phone OTP plus a custom Send SMS Hook and that Telnyx signs messaging webhooks with its timestamped Ed25519 envelope. Resend inbound events use signed webhooks, but the initial `email.received` event contains metadata rather than the message body/headers; the exact trustworthy sender-authentication evidence required by this prompt remains unproven and therefore blocks only the later email leg.
- The mandatory test-environment vendor spike cannot currently run. Neither local configuration nor any Corralio Vercel environment contains Telnyx API/sender/profile/public-key configuration, CAPTCHA configuration, or a dedicated channel-identity HMAC key. The repository also has no local Supabase test-project configuration, declared Telnyx hard-spend/segment cap, disposable test phone contract, or recorded A2P/10DLC campaign state. Corralio's Vercel project contains only its existing Supabase, routing/geocoding, VAPID, site, cron, and evidence-fingerprint configuration; no environment values were copied into the repository.
- Because the canonical prompt makes the capped live Telnyx/Supabase spike a sequential prerequisite, no phone-auth UI, SMS webhook, intake orchestration, channel-identity/pending-intake schema, migration, verifier, provider configuration, outbound SMS, database mutation, or application code was created. Proceeding would have contradicted the accepted abuse/cost and evidence gates.
- Smallest follow-up: a human provisions an isolated test Telnyx sender/profile and key, records the hard spend/segment cap and A2P status, provides a disposable test number, configures test Supabase phone Auth/Send SMS Hook plus CAPTCHA, and supplies a dedicated channel-HMAC secret. Then rerun Gate 3 with the predeclared cap. Resend webhook/sender-auth evidence can be audited later without blocking the SMS-first path.
- Verdict: **CORRALIO PHASE A+B BLOCKED AT TEST-ENVIRONMENT VENDOR SPIKE**. No migration was prepared or applied, and nothing was pushed or deployed.

## 2026-08-31 — Slice 3.6B Phase 1 complete locally

- A human applied `20260831_corralio_slice36b_required_arrival.sql`. The read-only catalog verifier returned `SLICE 3.6B PHASE 1 CATALOG VERIFICATION PASSED`, and the rollback-only behavioral verifier returned `SLICE 3.6B PHASE 1 BEHAVIORAL VERIFICATION PASSED; ROLLBACK CLEANUP ZERO`.
- The behavioral verifier's first invocation had stopped at its first preference-writer call because PostgreSQL does not implicitly narrow an integer fixture literal while resolving the exact `(uuid, smallint)` RPC signature. Commit `03296bd2` corrected only the verifier call sites with explicit `uuid`/`smallint` casts and added an offline architecture assertion; no migration, applied schema, authorization, or product code changed.
- Bounded signed-in browser UAT used a disposable confirmed Auth identity/household, one child/team with a 60-minute team preference, one assigned source, one unassigned household source, three synthetic weekend events, and precomputed geocode/route state. Both source cards exposed the same `Arrival setting` control without rendering their private URLs. The initial 45-minute assigned-source preference produced an 11:55 AM Leave-by; saving 35 minutes through the authenticated Family UI changed only that event to 12:05 PM. The feed-explicit event remained 9:00 AM throughout, proving explicit arrival still wins. Clearing the assigned override restored the team fallback and produced 11:40 AM. The unassigned source retained its independent 25-minute preference and 3:20 PM Leave-by.
- The final corrected UAT run made zero calendar fixture fetches and added zero Geocodio, OpenRouteService, Mapbox, Overture, HotelPlanner, push, or other provider rows; its browser resource list contained no provider URL, no framework error overlay appeared, and the local server returned successful authenticated Family/This Weekend responses. Existing engagement/What Fits writers created only their expected closed measurement rows, which were removed with the fixture.
- Before the clean rerun, an aborted harness setup supplied route state in the initial event insert; the existing provenance trigger correctly cleared it, so the mounted page recorded two non-billable Geocodio duplicate skips and one billable low-accuracy Geocodio rejection. No ORS or other provider ran. That disposable household was fully removed, the harness order was corrected to prime route state after event creation, and the complete rerun then held a zero-row provider baseline through closeout.
- Independent cleanup confirmed zero remaining disposable household, membership, child, team, source, event, schedule-connection measurement, weekly engagement, What Fits measurement, provider, quota, push-subscription, Weekend Ready campaign, temporary calendar, and Auth records. The local browser session and server were closed. No production data, canonical/provisional venue, deployment, push, or product configuration was changed.
- Verdict: **SLICE 3.6B PHASE 1 COMPLETE LOCALLY**. Arbiter Phase 2 remains separately inconclusive as already recorded, and the compound unknown-location ingestion risk remains outside this phase. Nothing was pushed or deployed.

## 2026-08-31 — Slice 3.6B Phase 1 required-arrival convergence ready for database verification

- Merged the founder's authoritative corrections into the canonical Slice 3.6B prompt and confirmed the repository starting facts. What Fits already honored a valid ICS/event `schedule_arrival_at` before team/default behavior, while This Weekend/Leave-by did not select that field and independently subtracted its own 30-minute default. Schedule sources had no arrival preference. This divergence is now resolved in repository code through one pure resolver with the exact hierarchy `ics_explicit → source_preference → team_preference → corralio_default` and typed in-memory provenance for all four tiers.
- Both consumers now use the same resolver. What Fits loads the bounded source preference in addition to its existing event/team inputs. This Weekend loads the existing feed-derived arrival timestamp plus source/team preferences, resolves the same required-arrival timestamp, and calculates Leave-by as resolved required arrival minus the cached estimated drive duration. Valid explicit arrival remains highest priority and unchanged. Clearing a source preference restores team/default behavior. No event, team, route, venue, or feed-derived arrival value is mutated.
- Added an `Arrival setting` control to each connected schedule using only the existing bounded source metadata plus nullable preference; private `source_url` remains absent from the connected-source projection. The authenticated Server Action validates null or 0–120 five-minute values and calls only the narrow owner-authorized RPC. The React/Next review kept the action authenticated, the client payload serializable, labels/control relationships accessible, and the static option list hoisted outside render.
- Prepared unapplied migration `20260831_corralio_slice36b_required_arrival.sql`. It adds only nullable constrained `smallint corralio_schedule_sources.arrival_buffer_minutes`, no backfill, forced RLS, the one additional authenticated select column, and `corralio_update_schedule_source_arrival_v1(uuid, smallint)`. The fixed-search-path definer RPC independently requires an active owner membership and returns only the saved bounded value. Authenticated clients receive no direct update grant; `anon`, `public`, and `service_role` receive no execute grant; source URL access is unchanged.
- Prepared the read-only catalog verifier and network-free rollback-only behavioral verifier. They cover column/constraint shape, forced RLS, column and function grants, locked function ownership/path, absence of URL access, assigned and unassigned sources, validation, direct-update denial, cross-household denial, clearing, unchanged team/feed arrival data, and rollback cleanup zero. They were not run because the migration is intentionally unapplied.
- Task 2 independently re-inspected the two sanitized real Arbiter fixtures. The Officials sample has a labeled `DESCRIPTION` structure but contains only soccer and uses the ambiguous literal `Sport: Tournament`; the second is a structurally incompatible football team/program export with only `Site:`. No newer genuine multi-sport Officials proving feed exists in the repository. Phase 2 therefore closes **inconclusive** and does not block Phase 1 or authorize a group parser/preference tier.
- Separately confirmed the current ingestion risk: Arbiter compound placeholder locations such as `(unknown) (unknown), (unknown) (unknown)  (unknown)` do not match the exact-value `NON_PLACE` exclusion. This required-arrival slice records but does not fix that venue/geocoding defect.
- No analytics event/schema, persisted provenance, dynamic household/source/team/event/time logging, provider-ledger vocabulary, provider call, calendar fetch, database mutation, reprocessing, Mapbox/traffic work, or Slice 3.6A behavior entered the change. Existing What Fits analytics remains on its closed applied vocabulary; `source_preference` provenance is intentionally sanitized to null rather than expanding measurement.
- Verification passed 35 focused required-arrival/Leave-by/What Fits/security tests, all 341 Corralio/shared-schedule tests, explicit Corralio TypeScript, zero-warning Corralio lint, React/Next review, `git diff --check`, and all four production builds (`corp-app`, `corralio-app`, `referee-app`, `ti-web`). RI/TI emitted only existing unrelated warnings. No database verifier, browser UAT, push, or deployment ran.
- Verdict: **SLICE 3.6B PHASE 1 READY FOR DATABASE VERIFICATION**. A human must apply the migration, then run both SQL verifiers and bounded local UAT with zero external-provider calls. Nothing was pushed or deployed.

## 2026-08-28 — HotelPlanner `getClientSummary` live diagnostic

- Made two founder-authorized read-only `getClientSummary` requests using the existing TI HotelPlanner credentials. The first returned HTTP 200 with three individual reservation records and zero group requests, meeting-space requests, approvals, or events; the second returned HTTP 200 with one individual reservation and zero records of the other types. No booking, change, cancellation, product, schema, cron, or deployment operation ran.
- The live response uses an authentication envelope with a nested `summary` object rather than the documented top-level collection shape. The nested summary supplied reservation, hotel, dates, numeric status, itinerary, pricing, and complete tracking metadata. No credential, authorization token, client contact detail, or reservation-specific detail was persisted in this note.
- Every record in the founder's first result carried a TI job code and TI tracking metadata, including keyword and Custom fields. A second founder-authorized read-only lookup returned one cancelled individual reservation with no job code or tracking fields. The founder confirmed that reservation also originated through TI's generic HotelPlanner white-label booking page, which does not populate the Custom fields. This establishes that `getClientSummary` covers TI white-label bookings beyond only fully attributed handoffs, while also proving that email presence alone cannot deterministically attribute a booking to a specific TI or Corralio handoff. Corralio's planned interface is expected to populate its bounded Custom-field chain, so Phase 3B reconciliation must require that Corralio attribution evidence rather than treating every email result as Corralio-originated.
- Added `scripts/ops/inspect-hotelplanner-client-summary.mjs` as a bounded ad hoc diagnostic. It accepts the email at runtime, reuses the existing HMAC/env convention, exposes no authorization material, redacts the response-envelope token from its optional owner-only temporary response file, and contains only the read-only `getClientSummary` method. Nothing was pushed or deployed.

## 2026-08-28 — Slice 3.6A Weekend Ready Web Push complete locally

- Founder/CPO approval converted the Stage 1 recommendation into the V1 decision: one primary opportunity Thursday at 4:37 PM in each household's confirmed IANA planning timezone, evaluated by bounded 15-minute UTC workers at `7,22,37,52 2-23 * * 4` and `7,22,37,52 0-6 * * 5`. The worker computes household-local eligibility from the stored timezone; null-timezone households remain disabled. One 90-minute transient retry is allowed, while campaign/delivery uniqueness prevents duplicate normal sends and provider-accepted deliveries are terminal.
- Added the dedicated Node Weekend Ready cron route and server-only `web-push` adapter. It reuses the existing exact `CRON_SECRET` bearer-auth, bounded database-claim, sanitized-result, and per-delivery failure-isolation patterns without coupling push work to schedule refresh. Trusted deep links and the VAPID subject derive only from validated `CORRALIO_SITE_URL`; caller-controlled host/origin headers are not trusted. The service worker contains only notification display/click handling and no cache, fetch interception, product-data storage, or private payload handling.
- Added the soft-ask experience only after meaningful authenticated weekend content. Permission is requested only from a user gesture; unsupported, denied, dismissed, iOS-not-installed, available, subscribed, unsubscribed, and bounded error states recover safely. When planning timezone is unset, the browser IANA zone is suggestion-only and requires explicit confirmation before enablement. Subscription capabilities travel only through owner-authorized same-origin Server Actions and service-role RPCs; the browser receives bounded status and never reads stored endpoint/key material.
- Notification content is exactly `Your weekend is ready` / `Open Corralio to see your family plan.` It contains no child, team, event, schedule, time, location, home/origin, conflict, tournament, What Fits, hotel, note, or source data. The static `/?src=weekend_ready_push` destination supplies deterministic notification context without claiming provider receipt or a cryptographically verified click.
- Preserved the applied two-level idempotency and privacy model: one campaign per household/planning weekend, one delivery per campaign/subscription fingerprint, no retry after provider acceptance, bounded transient retry, dead-endpoint revocation, membership-loss deactivation, forced RLS, service-only capability storage/writers, and the existing closed interaction vocabulary. Provider/delivery finalization failure is isolated so unrelated claimed deliveries continue. No analytics schema/device attributes, routing-provider ledger vocabulary, or schedule-refresh behavior changed.
- Controlled local browser UAT declared one disposable Auth identity/household, one credential-free synthetic schedule/event, at most one synthetic subscription, zero real provider pushes, and zero Geocodio/OpenRouteService calls. It proved the meaningful-content soft ask, safe denied and dismissed recovery, explicit timezone confirmation, subscribed/unsubscribe states through controlled browser-only Push API fixtures, iPhone Home Screen guidance without a permission request, a registered/controlling real `/sw.js`, safe service-worker headers, 401 cron rejection without authorization, exact 375×812 rendering with no horizontal overflow, zero application page errors, and zero axe violations. The only accessibility incomplete item was an existing automated contrast uncertainty caused by gradient/pseudo-element backgrounds.
- No real push was sent and no external push provider was called. One synthetic endpoint was persisted only through the controlled fixture and then revoked. Database inspection found zero campaigns, provider-ledger rows, quota rows, and active subscriptions. Independent cleanup returned zero disposable Auth, household, membership, source, event, subscription, campaign/delivery, interaction, provider/quota, and temporary-calendar records.
- Local `.env.local` has `CORRALIO_SITE_URL` and `CRON_SECRET` but does not contain `CORRALIO_VAPID_PUBLIC_KEY` or `CORRALIO_VAPID_PRIVATE_KEY`. Those secrets must be created/configured separately in the authorized deployment environments before deployment; no key material was retained by this slice. No production configuration was modified.
- Verification passed 294 Corralio/shared-schedule tests, explicit Corralio TypeScript, zero-warning Corralio lint, React/Next review, `git diff --check`, and all four production builds (`corp-app`, `corralio-app`, `referee-app`, `ti-web`). RI/TI emitted only their existing unrelated warnings. The signed-out surface remained push-free, and no schedule connection, freshness, conflict, leave-by, What Fits, venue, Mapbox/traffic, or 3.6B behavior was reopened.
- Actual iPhone/Android receipt, iOS Home Screen installation, lock-screen/OS presentation, background reliability, and native notification-tap handoff remain **UNVERIFIED ON PHYSICAL DEVICE** and are required for final pre-launch physical-device UAT. Email remains deliberately deferred pending push reach/opt-in/re-entry evidence and is not assigned to Slice 3.6B. Schedule-change push, Leave Soon alerts, live traffic/Mapbox, SMS, native apps, broad preferences, entitlement, and notification growth optimization were not built.
- Verdict: **SLICE 3.6A COMPLETE LOCALLY**. Nothing was pushed or deployed.

## 2026-08-28 — Slice 3.6A Weekend Ready Web Push database verification complete

- A human applied the base 3.6A migration and the catalog verifier passed. The first rollback-only behavioral run then exposed a real locked-search-path defect: the upsert/unsubscribe RPCs referenced `digest` without the Supabase `extensions` schema, so subscription upsert stopped before retaining fixture state. Additive forward repair `20260828_corralio_slice36a_digest_resolution_repair.sql` was applied without modifying the applied base migration; it changes no schema or product behavior, preserves the fixed function search path and service-only grants, and qualifies both calls as `extensions.digest`. The strengthened catalog verifier then returned `SLICE 3.6A CATALOG VERIFICATION PASSED`, and the behavioral verifier returned `SLICE 3.6A BEHAVIORAL VERIFICATION PASSED; ROLLBACK CLEANUP ZERO`.
- Filed the retired Push + Email draft and the founder-approved canonical Web Push-only prompt, then merged the final timezone, service-only subscription, two-level idempotency, bounded measurement, CSRF/origin, separate-worker, physical-device evidence, operational-logging, and trusted-URL corrections. Email remains a deliberate evidence-gated future decision owned by neither 3.6A nor 3.6B. No Mapbox/traffic, schedule-change push, Leave Soon, SMS, native app, entitlement, or broad notification-preference work entered the slice.
- The repository/PWA audit confirmed a complete standalone manifest but no service worker, registration, Push/Notification API usage, VAPID configuration, push dependency, subscription storage, notification UI, or product-email infrastructure. Existing schedule refresh is a separate protected four-hour cron and remains untouched; Stage 2 will reuse its exact bearer-auth, bounded-claim, and failure-isolation conventions through a distinct Weekend Ready route rather than coupling push latency/failure to refresh.
- The timezone audit found no household timezone column or setting. Applied-state inspection was aggregate-only: the one current household had 367 events, with 216 carrying `America/Los_Angeles` and 151 carrying no timezone; sampled canonical venues contained 12 timezone kinds. Event and venue timezones are destination evidence, not household truth, and private origin data is not repurposed. The existing weekend helper is explicitly browser-local, so Vercel/server locality cannot be treated as household-local truth.
- Founder superseded the temporary fixed-US workaround with a small authoritative household-timezone foundation. The applied migration adds nullable `planning_timezone` with no backfill; event/venue zones remain destination truth, leave-by remains absolute, and null-timezone households remain notification-ineligible. Browser `Intl` supplies only an explicit suggestion in the existing Family surface. Pure validation canonicalizes IANA identifiers; the hardened owner-scoped RPC independently validates against PostgreSQL timezone data. No event, venue, home address, coordinate, provider, server zone, or browser history can silently set the household value.
- Stage 1 recommends one primary opportunity Thursday at 4:37 PM household local time and a bounded 15-minute cadence during the global evaluation/retry envelope—`7,22,37,52 2-23 * * 4` plus `7,22,37,52 0-6 * * 5`, 116 invocations/week—for founder/CPO approval. The worker derives the small currently eligible-zone set, uses a partial household-timezone index, computes Friday-through-Monday absolute bounds in the confirmed IANA zone, limits household candidates/delivery insertion/claims to 50, and retains campaign/delivery uniqueness, `SKIP LOCKED`, terminal provider acceptance, and one 90-minute transient retry. This exact local time/cadence is the remaining product gate.
- Applied `20260828_corralio_slice36a_weekend_ready_push.sql`. Four forced-RLS service-only tables hold minimum subscription capabilities, closed permission interactions, one campaign per household/weekend, and one delivery per campaign/subscription fingerprint. The household timezone remains readable to its member but cannot be directly updated by authenticated clients; the narrow RPC authorizes the active owner. `corralio_external_api_calls` remains unchanged and routing-only.
- Updated read-only catalog verification, network-free rollback-only behavioral verification, and the aggregate usage report. The applied-state verifiers passed nullable/validated household timezone, restricted writer authorization, trusted digest resolution, explicit two-owner household isolation, event/household separation, null disablement, household-local claims, retry behavior outside the primary window, idempotency, rollback cleanup zero, and the existing push/privacy guarantees.
- Added the lightweight Family timezone confirmation/correction form and authenticated Server Action plus pure IANA/local-window/DST behavior. No `web-push` dependency, VAPID key, service worker, browser registration, push permission request, cron route/config, provider call, database write, push, or deployment was added. The proposed deep link remains static `src=weekend_ready_push`; it is notification context, not cryptographic click or delivery proof.
- Updated verification passed 16 focused timezone/Weekend Ready tests after the digest repair, all 286 Corralio/shared-schedule regression tests, explicit Corralio TypeScript, zero-warning Corralio lint, `git diff --check`, and all four production builds (`corp-app`, `corralio-app`, `referee-app`, `ti-web`). RI/TI emitted only their existing unrelated warnings. Database mutations were limited to the human-applied migration/repair and the behavioral verifier's fully rolled-back fixtures. No provider call, real subscription, notification, VAPID operation, cron invocation, push, or deployment occurred.
- Actual iPhone/Android receipt, iOS Home Screen installation, lock-screen/OS presentation, background reliability, and notification-tap handoff remain `UNVERIFIED ON PHYSICAL DEVICE` until final pre-launch physical-device UAT. Stage 2 may consume at most one separately declared real provider-accepted desktop/test push after the database and founder gates.
- Verdict: **SLICE 3.6A DATABASE VERIFICATION COMPLETE**. Founder/CPO approval of the exact 4:37 PM local time/cadence remains required before Stage 2 runtime implementation. Nothing was pushed or deployed.

## 2026-08-28 — Schedule Connection UX Unification complete locally

- Filed the founder-approved canonical prompt at `docs/prompts/corralio-schedule-connection-ux-unification-prompt.md`. The post-Slice-3.7 audit confirmed that household connection already used the typed platform catalog, team connection still exposed a raw-first calendar-link field, both paths shared the secure generic ICS ingestion pipeline, and the applied Slice 3.4 measurement constraint remained closed to the six post-3.7 platform keys. The work does not reopen Slice 3.7 parser, source-trust, refresh, assignment, venue, routing, or event-identity behavior.
- Expanded the serialized catalog to the exact seven-key founder matrix with `contexts: readonly ScheduleConnectionContext[]`. GameChanger, TeamSnap, Stack Team App, ArbiterLive, LeagueApps, and Other calendar are team-eligible; all seven sources, including Arbiter Officials, are household-eligible. Both flat pickers derive from one `getSchedulePlatformsForContext` boundary, with no duplicated matrix, `Set`, Common/More hierarchy, or search.
- Reviewed LeagueApps' official Calendar Sync documentation on 2026-08-28. It documents a recurring Subscribe to Calendar → Copy Link workflow, distinguishes subscription from one-time import, and describes a one-month-past/six-month-future window plus separate old-`RESCHEDULED` and new-event behavior. LeagueApps remains honestly `COMPATIBLE`, not `VERIFIED`, and uses the exact founder caveat: `If a LeagueApps game is rescheduled, its calendar feed may contain both the old game marked RESCHEDULED and the new game. Corralio hasn’t yet verified how that appears here, so double-check important changes directly in LeagueApps.` Its official support link is static HTTPS catalog metadata, contains no credentials/private feed data, and renders with `target="_blank"` plus `rel="noopener noreferrer"`.
- Unified the team-card connection experience around `Where does this team schedule live?`, a compact catalog-derived selector, shared expandable instructions, parent-facing `Paste calendar link` copy, the existing private-link reassurance/recovery semantics, and unchanged optional team creation. Instruction-view measurement now occurs when help is actually opened rather than merely when a platform is selected. No analytics schema/event/reason vocabulary or source URL handling changed.
- Added one pure typed `isSchedulePlatformAllowed(context, platform)` rule and the smallest injectable team orchestration. Both UI pickers and both Server Actions consume/enforce the canonical context rule. Deterministic tests prove a manipulated Arbiter Officials team submission is rejected before owner/team lookup, fetch, or persistence (zero dependency calls), while LeagueApps is accepted through the approved orchestration. Platform choice remains presentation/help/closed-measurement metadata and does not authorize a URL or alter fetch, parsing, SSRF, persistence, trust, venue, or assignment semantics.
- A human applied `20260828_corralio_schedule_connection_ux_unification.sql`, which only extends the existing closed Slice 3.4 platform constraint with `leagueapps`. The read-only catalog verifier returned `SCHEDULE CONNECTION UX CATALOG VERIFICATION PASSED`; the network-free rollback-only behavioral verifier recorded all seven keys, rejected an arbitrary key, and returned `SCHEDULE CONNECTION UX BEHAVIORAL VERIFICATION PASSED; ROLLBACK CLEANUP ZERO`. No table, column, RPC, policy, grant, event/reason vocabulary, or broad string acceptance was added.
- Local verification passed 19 focused catalog/orchestration/migration tests, all 253 Corralio/shared schedule tests, explicit Corralio TypeScript, zero-warning Corralio lint, the React/Next quality review, `git diff --check`, and production builds for all four workspaces (`corp-app`, `corralio-app`, `referee-app`, and `ti-web`). RI/TI emitted only existing unrelated warnings. Stage 1 made zero ICS fixture fetches, Geocodio calls, OpenRouteService calls, database mutations, provider-ledger changes, Auth/household fixtures, pushes, or deployments.
- Bounded signed-in browser UAT used one disposable confirmed Auth identity/household, one child/team, and two temporary public credential-free location-free ICS objects. The hard cap was four fixture GETs; exactly two successful no-redirect fixture GETs were consumed, while the controlled `ftp:` failure stopped before DNS/fetch and displayed the existing safe protocol recovery. Help reopened after failure, `Choose another schedule source` returned to the flat picker, and LeagueApps then connected at household level with the exact one-event success copy. The team saved independently before schedule connection, its picker contained the exact six team-eligible sources with Arbiter Officials absent, and LeagueApps connected with the same success copy while preserving `Schedule UX Child · Schedule UX Team` assignment. Both persisted sources were `success`; both private fixture URLs were absent from rendered inputs after connection.
- LeagueApps instructions, exact reschedule caveat, and static HTTPS official-help link were visible in both contexts; the link retained `target="_blank"` and `rel="noopener noreferrer"` and was not followed. The household picker retained all seven approved sources. Browser checks at 375×812, 430×932, and 393×851 CSS pixels found exact viewports, no horizontal overflow or framework overlay, distinct Save Team/Connect Team Schedule controls, a 44px help-summary target, and readable bounded help. Axe returned 26 passes, zero incomplete checks, and zero violations on the changed Family surface. Browser errors were empty; console output contained only React DevTools/Fast Refresh development notices.
- Aggregate post-UAT inspection found two active successful sources, two location-free events, one unassigned household source, one team-assigned source, and eight bounded interaction rows using only GameChanger/LeagueApps. Venue evaluation processed both location-free events with zero normalized locations, candidate queries, or provisional consideration/creation/reuse. Provider and quota ledger deltas were both zero: Geocodio `0`, OpenRouteService `0`, and no Overture or other provider work. Independent cleanup returned zero Auth, household, membership, child, team, source, event, measurement, engagement, provider, quota, and temporary-calendar rows/objects. Browser state, credentials, screenshots, harnesses, and the local server were removed.

### Outstanding UAT — LeagueApps Reschedule Behavior

- LeagueApps' documented old-`RESCHEDULED` plus new-event behavior has not been exercised against a representative Corralio-side feed. Do not claim verified cancellation/reschedule semantics until that bounded UAT exists; the exact in-product caveat above remains operative.
- Verdict: **SCHEDULE CONNECTION UX UNIFICATION COMPLETE LOCALLY**. LeagueApps representative-feed reschedule behavior and Arbiter Officials populated-feed lifecycle behavior remain honestly deferred; neither blocks this completed UX/catalog slice.

## 2026-08-28 — Slice 3.7 Arbiter schedule sources complete locally

- Filed the corrected canonical prompt at `docs/prompts/corralio-slice-3.7-arbiterlive-arbiter-officials-schedule-sources.md`. The repository audit reproduced both defects against `node-ical`: parameterized `SUMMARY`, `LOCATION`, and `DESCRIPTION` properties arrive as `{ params, val }` objects, while an empty valid feed produces a `VCALENDAR` component, arbitrary text produces no calendar structure, and the existing bare-`VEVENT` shape remains parseable. It also confirmed that initial ingestion rejected zero normalized events before source creation, replacement intentionally rejects zero-event feeds, scheduled refresh already accepts valid-empty results, unassigned sources/events are supported, and conflict derivation has no assignment gate.
- Added one internal ICS-property extractor with a closed contract: plain strings pass through, objects yield only a string `.val`, and missing/non-string values become empty text rather than `[object Object]`. The generic normalizer now applies it to summary, location, and description. Calendar validity now requires a parsed `VCALENDAR` or `VEVENT`, preserving malformed-input rejection and the existing bare-event compatibility.
- Initial ingestion now persists a structurally valid zero-upcoming-event source and returns `imported: 0`, allowing the established refresh/manual-refresh pipeline to discover later assignments. Both household and team Server Actions render the exact success copy `Schedule connected — no upcoming events were found yet.` Replacement remains deliberately stricter: a valid-empty replacement returns the existing safe `no_events` failure and cannot displace a working URL.
- Expanded the typed/versioned picker to six choices. ArbiterLive uses the neutral recognition `For school team schedules`, the screenshot-confirmed follow/email instructions, and an explicit email-round-trip caveat. `Arbiter (Officials)` uses the documented Settings → Preferences → Calendar Sync → Send Email path and the exact caveat: `This connects your own officiating assignments. We haven’t yet confirmed how ArbiterSports reports declined, reassigned, or canceled games in this feed. Until that’s verified, double-check any important change directly in ArbiterSports.` Both remain `COMPATIBLE`; no partnership, provider-specific parser/fetch path, trust evidence, source URL exposure, or new analytics vocabulary was added.
- A human applied migration `20260828_corralio_slice37_arbiter_schedule_sources.sql`, which only widens the existing Slice 3.4 platform check constraint from four approved keys to the exact six-key catalog. The read-only catalog verifier returned `SLICE 3.7 CATALOG VERIFICATION PASSED`; the rollback-only behavioral verifier recorded both new platform keys and returned `SLICE 3.7 BEHAVIORAL VERIFICATION PASSED; ROLLBACK CLEANUP ZERO`. No column, table, event/reason vocabulary, writer, grant, RLS policy, schedule/event schema, or refresh behavior changed.
- The repaired team-calendar shortcut remains intentionally outside this slice’s picker/help work: it shares the corrected parser/ingestion path but still has no platform selection and therefore cannot display the Officials lifecycle caveat. That known UX gap is deliberately deferred from Slice 3.7 and requires a separately authorized, repository-filed UX-unification slice before implementation.
- Verification passed 47 focused parser/ingestion/catalog/conflict tests, all 263 Corralio/shared schedule tests, explicit Corralio TypeScript, zero-warning Corralio lint, `git diff --check`, and all four production builds (`corp-app`, `corralio-app`, `referee-app`, and `ti-web`). RI/TI emitted only existing unrelated warnings. The behavioral fixtures rolled back and cleanup reached zero. No feed was fetched, provider called, push, or deployment performed.

### Outstanding UAT — Arbiter Officials Lifecycle Semantics

- Decline, reassignment, and cancellation behavior in populated Arbiter Officials feeds remains unverified. The available Officials sample had zero games and could not exercise these transitions; the documented Settings/Preferences discovery path has also not been walked hands-on. Bounded live UAT against a populated feed is required before either behavior can be called verified. Until then, the exact in-product caveat above is the operative disclosure. This is a tracked follow-up, not a resolved item.
- Verdict: **SLICE 3.7 COMPLETE LOCALLY**. Populated-feed lifecycle UAT remains explicitly outstanding without reopening this completed implementation slice.

## 2026-08-28 — Team editing and team-calendar connection production repair prepared locally

- Investigated the signed-in production failure without submitting another calendar or changing production data. The current Corralio deployment returned safe 200 Server Action responses, while Vercel retained the exact bounded ingestion signal: `stage: 'create_source', code: '23514'`. Repository inspection proved two independent gaps. Team-calendar creation passed both the owning child ID and team ID into the original exactly-one assignment writer, while the established assignment model derives child ownership from a persisted team-only assignment. Separately, Slice 4.1B had narrowed authenticated team updates before Slice 4.6 introduced `arrival_buffer_minutes`, and Slice 4.6 had not restored update permission for that new private column.
- Corrected the trusted schedule-store adapter so child-only connections still persist `child_id`, while team connections pass `p_child_id = null` and persist only the server-validated `team_id`. No browser-supplied household/child relationship, new RPC, assignment constraint change, source-URL disclosure, fetch behavior, parser behavior, or canonical persistence behavior was introduced.
- Added unapplied migration `20260828_corralio_team_schedule_connection_fix.sql`, granting authenticated users update access only to `corralio_teams.arrival_buffer_minutes`; it does not restore broad table update access or expose the column to anonymous users. Strengthened the Slice 4.6 catalog verifier to require that exact grant and deny it to `anon`. Strengthened the rollback-only behavioral verifier to prove an authenticated owner can update the arrival preference and create a team-only source through the existing writer.
- Verification passed 18 focused assignment/team/What Fits tests, all 255 Corralio/shared schedule tests, Corralio TypeScript, zero-warning Corralio lint, `git diff --check`, and all four production builds (`corp-app`, `corralio-app`, `referee-app`, and `ti-web`). RI/TI emitted only existing unrelated warnings. No migration was applied, production row changed, calendar fetched, push, or deployment performed. Production repair still requires human migration application, both updated Slice 4.6 SQL verifiers, and a separately authorized push/deployment before the Avery Sports flow can pass in production.

## 2026-08-27 — Slice 3.5.5 schedule freshness complete locally

- A human applied the Slice 3.5.5 migration. The catalog verifier returned `SLICE 3.5.5 CATALOG VERIFICATION PASSED`, and the rollback-only behavioral verifier returned `SLICE 3.5.5 BEHAVIORAL VERIFICATION PASSED; ROLLBACK CLEANUP ZERO`. Concurrency, shared cron/manual claims, busy/unavailable outcomes, household isolation, five-minute cooldown, three-hour automatic eligibility, and the 24-hour failure window were proven without invoking or racing production cron.
- Bounded browser UAT used one disposable Auth/household fixture and credential-free ICS data. The declared four-attempt ceiling was consumed conservatively: three HTTP GETs were confirmed, while one localhost-fixture attempt was rejected by the existing SSRF boundary before a GET. The established historical fixture safely returned zero upcoming events; the final changed fixture returned two upcoming events, replaced the stale seed, and persisted two unique UIDs despite a repeated UID payload. Existing deterministic re-import/refresh tests cover the repeated-input boundary without a fifth fetch.
- Family rendered successful freshness, immediate cooldown, safe transient-failure copy, and persistent paused/replacement guidance. This Weekend rendered one conservative oldest-success aggregate. Authenticated browser access to private refresh columns and direct service-only claims was denied with `42501`; rendered data contained no source URL, `.ics` reference, claim token, secret parameter, provider error, or exception. Browser checks at 375×812 and 1280×900 found no horizontal overflow, application console error, or page error.
- Both disposable events matched existing read-only canonical venues; zero provisional venue was referenced, created, or reused. No canonical venue changed, no venue promotion/reconciliation or Overture refresh ran, and production cron was not invoked. ICS remains unmetered: fixture provider/quota rows and global existing-ledger deltas were exactly zero.
- Cleanup independently returned zero disposable household, membership, child/team, source, event, venue-match, schedule-connection interaction, engagement, What Fits, provider/quota, Auth, and temporary calendar-object rows. Browser profiles/sessions, screenshots, local servers, state, credentials, and harness files were removed.
- Final verification passed all 254 Corralio/shared schedule tests, Corralio TypeScript, zero-warning Corralio lint, `git diff --check`, and all four production builds (`corp-app`, `corralio-app`, `referee-app`, and `ti-web`); RI/TI emitted only existing unrelated warnings. Detailed evidence is in `docs/reports/corralio-slice-3.5.5-stage2-2026-08-27.md`. Verdict: **SLICE 3.5.5 COMPLETE LOCALLY**. No push or deployment occurred.

## 2026-08-27 — Slice 3.5.5 schedule freshness ready for database verification

- Merged the approved specification and clarifications into `docs/prompts/2026-08-27-slice-3.5.5-schedule-freshness-prompt.md`. Stage 1 used aggregate-only repository, database, and Vercel inspection and retained no household/source identifier, URL, feed content, event text, or private location. It found four active, eligible, unpaused ICS sources, all in `success`, with no attempt in the preceding 24 hours. Recent batch history is **UNPROVEN** because no suitable retained history exists; no logging was added to manufacture one.
- Confirmed the effective 23-hour gate is inside the applied service-only RPC, not the TypeScript constant. Prepared unapplied migration `20260827_corralio_slice355_schedule_freshness.sql` to use the approved three-hour eligibility under `17 */4 * * *`, while retaining deterministic `FOR UPDATE SKIP LOCKED` claiming, a ten-minute claim timeout, sequential processing, and the hard batch cap of ten. Vercel Pro and the registered prior daily cron were confirmed, but no retained schedule-refresh invocation was found, so cron execution remains a Stage 2 proof rather than an inference from registration.
- Current capacity fits in one batch. The new bound is six invocations and 60 claimed feed fetches/day; at four sources the projected maximum is 24 fetches/day, up to 20 more than the old daily cadence. ICS fetches are not metered in the existing provider/quota ledger. Stage 1 made exactly zero fixture/feed fetches and added no analytics, provider, quota, cost, or execution-history instrumentation.
- Preserved the three-failure threshold while adding a private failure-sequence start so a source cannot pause until at least 24 hours have elapsed; rapid four-hour polling cannot pause a source after only 8–12 hours. Successful automatic/manual canonical persistence and validated replacement reset the sequence. Paused sources keep the existing replace-link recovery and cannot be manually retried in V1.
- Added a service-only, household/source-bound manual claim with a five-minute per-source database cooldown, no household daily cap, and no cost throttle. It shares cron's claim state and returns the source URL/token only inside trusted server code. The authenticated Server Action reuses the exact existing fetch/normalize/persist/finalize/venue-matching pipeline for one claim and returns only bounded safe outcomes; source credentials, feed/provider errors, and raw exceptions never enter browser data.
- Family now shows per-source successful freshness and failure-aware last-success copy; This Weekend has one conservative aggregate freshness line. `last_synced_at` remains the successful timestamp and `last_refresh_attempted_at` remains attempt metadata. Relative-time logic uses fixed/injected clocks and a serialized server reference time to prevent hydration-time drift.
- Prepared a read-only catalog verifier and a network-free rollback-only behavioral verifier covering grants/ownership, private metadata, cadence/caps, cooldown, household isolation, cron/manual exclusion, failure timing, paused behavior, and cleanup zero. No migration was applied, SQL verifier run, feed fetched, production cron invoked, event reprocessed, provider called, push, or deployment performed.
- Stage 1 verification passed all 254 Corralio/shared schedule tests, Corralio TypeScript, zero-warning Corralio lint, React/Next review, `git diff --check`, and all four production builds (`corp-app`, `corralio-app`, `referee-app`, and `ti-web`); RI/TI emitted only existing unrelated warnings. Detailed evidence is in `docs/reports/corralio-slice-3.5.5-stage1-2026-08-27.md`. Verdict: **SLICE 3.5.5 READY FOR DATABASE VERIFICATION**.

## 2026-08-27 — Slice 3.5 Mobile Experience Hardening complete locally

- Filed the corrected canonical prompt at `docs/prompts/corralio-slice-3.5-mobile-experience-hardening.md`, preserving the automated-versus-physical evidence boundary, disposable fixture/provider contract, existing-only analytics rule, and no-push/no-deploy scope. Direct agent-browser/CDP viewport control verified 375×812, 430×932, and 393×851 mobile layouts plus an 812×375 landscape smoke; exact `innerWidth`/`innerHeight` checks proved the viewports, so no iframe fallback was used.
- The audit found and fixed bounded launch issues rather than redesigning the product: a failed mobile connection left the platform picker 163px above the viewport with no explicit switch action; new source selection could resurrect stale error copy; password/auth and What Fits controls fell below 44px; signed-out account actions used an invalid generic ARIA label; conflict overlap clocks disagreed with event-timezone cards; sport emoji lacked a semantic role; dark What Fits controls failed contrast at 3.23:1; and Family assignment labels measured 4.4:1. The corrected surfaces have zero measured sub-44px actions, zero horizontal overflow, and zero axe violations in the tested light/dark screens.
- End-to-end UAT used the existing smoke Auth identity with one disposable household, child/team, two credential-free public ICS fixtures, two assigned schedule sources, four current-weekend events, two existing read-only canonical venues, and the active reviewed 4.5A pools. It covered private-link recovery, GameChanger and TeamSnap success, connect-another, connected-source management, one same-child conflict, four leave-by lines, Food default, Top 3 plus six-result See More, Coffee lazy loading, candidate selection, and the directions chooser. Apple Maps, Google Maps, and Waze hosts/query presence/target/rel were inspected without external navigation; Cancel returned to This Weekend.
- No canonical/provisional venue was created or modified, no venue was promoted/reconciled, no Overture refresh ran, and Geocodio usage was zero. A declared 30-call ORS ceiling was exceeded when repeated This Weekend reloads during hot-reload verification triggered 48 `route_what_fits` attempts (47 success, one error) before the ledger check. Further provider-triggering reloads stopped immediately; remaining viewport/theme tests reused the rendered DOM. This operational deviation is retained explicitly in the closeout report rather than hidden, and no routing-cache/provider architecture change was added to compensate for it.
- Existing Slice 3.4/4.6 measurement only recorded bounded interactions; no schema, vocabulary, writer, device attribute, or viewport fingerprint was added. Cleanup validated the exact UAT ownership first, then independently returned zero household membership, schedules, events, connection interactions, engagement, What Fits analytics, provider/quota rows, temporary calendar objects, and disposable Auth fixtures while preserving the shared smoke Auth identity.
- Physical-device-only behavior is **UNVERIFIED ON PHYSICAL DEVICE**: native software-keyboard obstruction, cross-app paste, hardware safe areas, native Maps handoff, and OS return behavior. These remain final pre-pilot physical-device UAT after Slice 3.6, not blockers to this automated slice.
- Final verification passed all 248 Corralio/shared schedule tests, Corralio TypeScript, zero-warning Corralio lint, `git diff --check`, and all four production builds (`corp-app`, `corralio-app`, `referee-app`, and `ti-web`); RI/TI emitted only their existing unrelated warnings. Detailed evidence is in `docs/reports/corralio-slice-3.5-mobile-hardening-2026-08-27.md`. Verdict: **SLICE 3.5 COMPLETE LOCALLY**. No push or deployment occurred, and Slice 3.6 was not begun.

## 2026-08-27 — Slice 3.4 Schedule Connection Activation complete locally

- Closed the missing Stage 1 audit with bounded, read-only evidence from the one existing household containing all three launch platforms. GameChanger has 150 current events, TeamSnap has 177 across two sources, and Stack Team App has 39; every source is in `success`, has zero current refresh failures, and has no duplicate persisted UID. Location presence is 148/150, 177/177, and 39/39 respectively. Two GameChanger rows begin at local midnight and remain an audit flag rather than a claimed parser defect. No household/source ID, subscription URL, feed content, event text, note, or private location value was printed or retained, and the audit made zero database writes and zero feed/provider calls. All three named platforms remain honestly `COMPATIBLE`, not `VERIFIED`; Other calendar is `MANUAL`.
- Implemented the exact four-choice `Where does this schedule live?` picker for GameChanger, TeamSnap, Stack Team App (supporting recognition: `Also called Sports Connect`), and Other calendar from one typed/versioned code catalog. The selected platform is a closed activation-only enum and does not alter the existing SSRF-safe fetcher, parser, persistence, refresh, venue, or Slice 4.4B trust boundaries. No SportsEngine, TomTom, notification, mobile-hardening, OAuth, or direct-platform API work was added.
- Added a closed safe connection-error kind while preserving parent-safe messages. Private/local and non-ICS failures use the approved exact copy and render contextual recovery help. Success now says `Schedule connected — we found [N] upcoming events` and offers both `Connect another schedule` and `See This Weekend` without a forced redirect. Two signed-out fixture cards carry restrained static `via GameChanger` and `via TeamSnap` labels with no data or provider dependency.
- Reconciled the funnel with Slice 4.2A. Successful imports, active-source counts, second-schedule activation, and This Weekend usage remain report-time state from existing tables. Prepared, but did not apply, `20260827_corralio_slice34_schedule_connection_activation.sql` for only the non-derivable interactions: platform selected, instructions viewed, submission failure, and validation failure. Its authenticated-owner RPC accepts closed event/platform/reason vocabularies, minute-deduplicates, applies a 180-day per-household retention policy on subsequent interaction, exposes no client table access, and is wrapped by constant payload-free fail-open application logging. The read-only report keeps interaction counts separate from derived activation.
- A human applied `20260827_corralio_slice34_schedule_connection_activation.sql`. The read-only catalog verifier returned `SLICE 3.4 CATALOG VERIFICATION PASSED`, and the rollback-only behavioral verifier returned `SLICE 3.4 BEHAVIORAL VERIFICATION PASSED; ROLLBACK CLEANUP ZERO`.
- Signed-out browser verification confirmed the static source labels and existing landing hierarchy. Final signed-in UAT used the repository smoke identity only after proving it had zero Corralio households, plus two synthetic public ICS objects in the existing `ti-assets` bucket. GameChanger first rejected a private localhost URL with the exact contextual recovery and no source/event creation, then imported one Soccer event. `Connect another schedule` continued into Other calendar, which imported one Hockey event. Success exposed both continuation choices, the Family screen showed exactly two successful connected schedules without exposing either URL, and `See This Weekend` rendered exactly the two synthetic Saturday/Sunday events with their source labels and sports. The browser had no application errors.
- Retained-state inspection before cleanup proved exactly two successful sources, two events, and one derived weekly-engagement row. The interaction table contained only one each of GameChanger selected/instructions/private-URL validation failure and Other selected/instructions; there was no duplicate successful-import, second-schedule, or weekend-view interaction event. All source URLs matched the exact temporary fixture prefix, and `corralio_external_api_calls` remained zero. Because both fixtures omitted location, venue matching examined zero normalized locations and made zero candidate queries, provisional decisions, geocoding calls, or routing calls.
- Cleanup first proved the household contained only the two named fixture sources and events, then deleted that exact disposable household and the two exact storage objects while preserving the shared smoke Auth identity. Independent post-cleanup reads returned zero membership, source, event, interaction, weekly-engagement, external-call, and storage-object rows.
- Stage 1 verification passed all 232 then-current Corralio/shared schedule tests, Corralio TypeScript, zero-warning Corralio lint, React/Next review, `git diff --check`, and all four production builds. Final closeout verification passed all 246 current Corralio/shared schedule tests, Corralio TypeScript, zero-warning Corralio lint, `git diff --check`, and all four production builds (`corp-app`, `corralio-app`, `referee-app`, and `ti-web`); RI/TI emitted only their existing unrelated warnings. No retained fixture, provider/geocoding/routing usage, push, or deployment occurred.
- Verdict: **SLICE 3.4 COMPLETE LOCALLY**.

## 2026-08-26 — Homepage messaging and trademark refresh complete locally

- Updated the canonical non-slice Section K prompt with the approved specification corrections, then implemented only that bounded signed-out marketing work. The existing hero remains `The planner built for sports families.` followed by `Every kid. Every team. One plan.`; the compact bridge after the example weekend now adds `Corral your sports chaos.`, `Every schedule, every kid, every team — brought together so you can see how the weekend actually works.`, and `Bring your schedules together. Spot conflicts. Know what the weekend looks like.` before the unchanged account-access form.
- Added exactly one presentation-only fixture line to Jordan's synthetic Saturday event using the production-exact copy `Leave by 6:55 AM (est.) · ~52 min estimated drive`. The signed-out preview remains a pure Server Component fixture with no imported authenticated calculation, client boundary, Supabase/product-data/database access, fetch, routing, geocoding, provider, or external-call path.
- Added one restrained `™` superscript to the textual Corralio eyebrow following the existing logo, using the established `--accent` brand token. No logo component or asset was wrapped, altered, replaced, recolored, or resized; ordinary body references remain unmarked and the tagline has no trademark symbol. No footer ownership/legal-entity statement was added because the inspected Corralio surface establishes no authoritative owner.
- Existing Get Started and Sign in anchors and the combined magic-link/password account flow remain unchanged. The repository has no existing signed-out homepage analytics instrumentation, so none was added, removed, renamed, or duplicated.
- Browser verification passed at 1280×900 and 390×844 with the expected hierarchy, natural tagline/leave-by wrapping, correctly positioned trademark, reachable form, working anchor focus, zero horizontal overflow, no framework overlay, no page errors, and only the standard React DevTools development notice. The accessibility audit returned zero violations. Its complete network ledger contained only localhost document/static assets (font, unchanged logos, CSS/JS, manifest, and icons), proving zero Supabase, provider, geocoding, routing, or other external request from the preview.
- Final verification passed all 240 Corralio/shared schedule tests, Corralio TypeScript, zero-warning Corralio lint, `git diff --check`, and all four production builds (`corp-app`, `corralio-app`, `referee-app`, and `ti-web`); RI/TI emitted only their pre-existing warnings. No form was submitted, database/provider call or retained-data mutation occurred, and no push or deployment occurred.

## 2026-08-26 — Slice 4.6 What Fits complete locally

- A human applied `20260826_corralio_slice46_what_fits.sql`. The catalog verifier returned `SLICE 4.6 CATALOG VERIFICATION PASSED`; after correcting its synthetic schedule-source fixture to satisfy the existing exactly-one child/team assignment constraint, the rollback-only behavioral verifier returned `SLICE 4.6 BEHAVIORAL VERIFICATION PASSED; ROLLBACK CLEANUP ZERO`.
- Bounded signed-in browser UAT used two disposable Auth identities/households and existing read-only public canonical venues with active 4.5A pools. It covered a roomy different-venue gap, explicit ICS arrival, a 45-minute team preference, Food no-fit/Coffee fit, exact no-fit, below-threshold suppression, household-conflict suppression, Food-default/Coffee-lazy behavior, Top 3 plus See More, navigation chooser, `status_unknown` honesty, and desktop/mobile layouts. It created or modified no canonical/provisional venue, performed no promotion or Overture extraction, and left the active 87-candidate 4.5A pool unchanged.
- The reviewed roomy pool surfaced plausible Food options (Taco Bell, Wendy's, Arby's, KFC, Subway, and Burger King) and Coffee options (Tim Hortons and Dunkin' among the ordered results). Questionable brewery/dessert candidates and duplicates did not leak into the recommendation UI. The 50-minute fixture truthfully produced Food no-fit while two Coffee stops fit; the 45-minute fixture produced combined no-fit; 44 minutes and a sibling conflict suppressed the module. Family UAT confirmed the exact 0–120-minute five-minute-step selector with 45 minutes selected.
- Browser UAT exposed and fixed one real presentation defect: Arrive-by and Leave-by were formatted in the browser timezone rather than the next event's timezone. The server result now carries the next-event timezone and the client formats both clocks against it with a safe fallback. The corrected Detroit fixture displayed the 4:00 PM event, 3:30 PM required arrival, and approximately 3:00 PM candidate leave-by consistently.
- Audited UAT usage was 120 OpenRouteService `route_what_fits` attempts (117 success and 3 isolated retryable failures) plus one fixture-caused Geocodio `geocode_event` failure before the sibling-conflict fixture received its known coordinate. The corrected conflict repeat added zero calls. One disposable household quota row was reset once to isolate the later no-fit scenarios. Analytics recorded 7 eligible/surfaced/viewed gaps, 26 candidates shown, 2 See More opens, 3 mode selections, 3 no-fit outcomes, 1 candidate/directions selection, and 2 suppressions (`below_minimum_gap` and `household_conflict`); no private payload columns were present.
- Cleanup removed the exact disposable provider/quota/analytics, household, schedule/event, browser-state, and Auth fixtures. The harness returned `SLICE 4.6 UAT CLEANUP ZERO`, and an independent retained-state read returned zero fixture households, events, sources, and Auth users. Final verification passed all 240 Corralio/shared schedule tests, Corralio TypeScript, zero-warning Corralio lint, `git diff --check`, and all four production builds (`corp-app`, `corralio-app`, `referee-app`, and `ti-web`); RI/TI emitted only their pre-existing warnings.
- Verdict: **SLICE 4.6 COMPLETE LOCALLY**. No push or deployment occurred.

## 2026-08-26 — Slice 4.6 What Fits Stage 2 implementation ready for database verification

- Implemented the approved V1 What Fits experience inside This Weekend under the canonical prompt and the durable 15-item CPO decision packet, including its brewery amendment. The deterministic policy uses the exact arrival hierarchy (structured ICS clock → optional team setting → 30-minute default), 45-minute household-aware raw-gap threshold, approved Food/Coffee dwell times, full current-event → candidate → next-event routing arithmetic, conservative household-conflict suppression, V2 4.5A quality/status gates, and the approved ranking/tie behavior. Brewery remains an eligible internal Food subtype with a 45-minute dwell and loses an otherwise-equal Other Food tie; no brewery wording or mode appears in the UI.
- Added the parent-facing Food-default/Coffee-lazy module with the approved available-time and Arrive-by provenance semantics, Best 3 plus bounded See More, `Fits your schedule`, actionable candidate leave-by, estimated/no-live-traffic honesty, `Hours not verified` for eligible unknown status, mode-specific no-fit copy, and the combined no-fit state. Missing venues/pools, household ambiguity, routing failure, and quota exhaustion suppress enrichment without degrading the weekend plan. Directions continue through the established navigation chooser; no search, generic Nearby, hotel/travel, live traffic, ML, new venue architecture, or new ingestion path was added.
- Reused the existing server-only OpenRouteService adapter, atomic 50-call daily household quota, and payload-free provider ledger. Each evaluated mode prefilters at most six active V2 candidates, routes at most 12 unique directed legs with concurrency three and no retry or new persistent cache, and evaluates only the most relevant upcoming safe gap per render. A legitimately empty successfully activated 4.5A scope produces a real no-fit result; absent or failed pool state suppresses the module. Initial Food evaluation follows the existing leave-by enrichment pass; Coffee routes only after explicit selection.
- Added a nullable 0–120-minute, five-minute-step team arrival preference and deterministic structured ICS `Arrival Time:`/`Arrival:` parsing limited to exact AM/PM clocks 0–180 minutes before start. Prepared, but did not apply, `20260826_corralio_slice46_what_fits.sql`, which adds those private fields, the `route_what_fits` audit operation, a forced-RLS/no-policy sanitized analytics table, an authenticated owner-derived writer, and the updated service-only canonical ICS persistence function. Prepared a read-only catalog verifier and a network-free rollback-only behavioral verifier with an independent post-rollback cleanup-zero assertion.
- Automated verification passed all 240 Corralio/shared schedule tests, including literal Section 96 arrival, threshold, household-safety, Food/Coffee, route/dwell, status, ranking, result-count/See-More, no-fit, failure, and analytics-redaction boundaries. Corralio TypeScript, zero-warning Corralio lint, `git diff --check`, and all four production builds (`corp-app`, `corralio-app`, `referee-app`, and `ti-web`) passed; RI/TI emitted only their pre-existing warnings. React/Next review preserved authenticated Server Action boundaries and kept provider/database access out of client code.
- No migration, SQL verifier, browser/provider UAT, database mutation, route/geocode/Overture call, cron, backfill, push, or deployment occurred in this implementation pass. Stage 2 cannot receive a canonical terminal verdict until a human applies the migration, both SQL verifiers pass, Sections 99–102 browser/UAT use reviewed real 4.5A candidates with disposable household events, provider/analytics usage is audited, and fixture cleanup is independently zero.
- Intermediate state: **SLICE 4.6 READY FOR DATABASE VERIFICATION**. This is not the final Slice 4.6 verdict.

## 2026-08-26 — Slice 4.4D incomplete ICS venue resolution complete locally

- A human applied `20260826_corralio_slice44d_incomplete_ics_venue_resolution.sql`; the catalog verifier returned `SLICE 4.4D CATALOG VERIFICATION PASSED`, and the rollback-only behavioral verifier returned `SLICE 4.4D BEHAVIORAL VERIFICATION PASSED; ROLLBACK CLEANUP ZERO`.
- The bounded no-write prediction examined 200 ICS events and 24 normalized locations, predicting 159 canonical matches, 39 unmatched locations, and 2 insufficient locations with zero retained match, alias, or external-call changes. After explicit approval, the same 200-event bound was applied through the production matcher. A final exact 32-event bounded pass completed the later Plantes Ferry regression occurrences that fell beyond the chronological first batch.
- Final retained reads prove every required production-representative occurrence resolves with `corralio-v2`: all 50 Upriver/Plantes Ferry events reference the existing Plantes Ferry Sports Complex canonical venue, and all 32 Eagles Ice Arena events reference the existing Eagles Ice Arena canonical venue. There are zero absent/older target associations, 12 validated shared aliases, and zero provisional venues; no canonical venue was created or modified.
- Isolated signed-in browser UAT covered This Weekend, Family, and Upcoming without entering credentials. Four schedules, the existing conflict, raw Upriver and Eagles location text/directions controls, household-origin privacy copy, family/schedule connections, and the Upcoming placeholder rendered correctly. The browser reported no application error or exception; the temporary browser, auth runner, screenshots, and dev server were cleaned up.
- Stage 2 and UAT made four billable Geocodio `geocode_event` attempts after event geocoding was deliberately decoupled from household-origin routing: one succeeded, two returned `invalid_result`, and one returned `low_accuracy`. There were zero OpenRouteService and Overture calls, zero provisional/canonical writes, and no source-feed fetch, cron, background processing, push, or deployment.
- Final validation passed 163 current Corralio library tests, Corralio TypeScript, zero-warning Corralio lint, `git diff --check`, and all four production builds (`corp-app`, `corralio-app`, `referee-app`, and `ti-web`); RI/TI emitted only their existing warnings.
- Verdict: **SLICE 4.4D COMPLETE LOCALLY**. Resume the existing Slice 4.6 Stage 1 audit and stop at its CPO decision packet before any What Fits Stage 2 implementation.

## 2026-08-26 — Slice 4.4D incomplete ICS venue resolution Stage 1 ready

- The 4.6 audit confirmed a production-representative resolution gap rather than missing canonical data: both Plantes Ferry Sports Complex and Eagles Ice Arena already exist in the TI/RI canonical venue base, while the retained ICS strings are respectively address-only and name-only with `(home ice)`. Existing matching rejected the latter and compared the former too literally; all 82 retained occurrences remain untouched in the applied database.
- Implemented versioned `corralio-v2` matching in repository code. It strips only an approved bounded annotation vocabulary, supports complete unique canonical-name resolution, compares exact normalized street identity within city/state while ignoring harmless street-type/ZIP formatting, rejects different street numbers and ambiguous names, and uses validated aliases only after ordinary canonical matching. No global/name-only Overture search was added; unsupported or ambiguous locations remain unresolved.
- Prepared, but did not apply, `20260826_corralio_slice44d_incomplete_ics_venue_resolution.sql`. It adds a forced-RLS, service-only alias relation with exactly-one canonical/provisional integrity and an indexed service-only unique canonical-name RPC. It creates or modifies no canonical/provisional venue and performs no backfill. Prepared machine-failing catalog and rollback-only behavioral verifiers; they remain intentionally unrun until human migration application.
- Event geocoding now runs through the existing bounded claim/deduplication/quota path before the optional household-origin routing gate. Venue matching remains best effort, and ORS is not required or called when no home origin exists. Added an explicit bounded reprocessor with dry-run required by its caller, a 200-event cap, aggregate-only results, deterministic ordering, and no automatic invocation.
- Focused verification and the complete 210-test Corralio suite passed, along with Corralio TypeScript, zero-warning lint, `git diff --check`, and production builds for all four workspaces (`corp-app`, `corralio-app`, `referee-app`, and `ti-web`); RI/TI emitted only pre-existing warnings. No applied-database mutation, event reprocessing, source-feed fetch, Geocodio/ORS/Overture call, canonical/provisional write, cron, push, or deployment occurred. Stage 2 requires human migration application, both SQL verifiers, bounded dry-run, explicit apply approval, regression/UAT, cleanup-zero confirmation, a repeat of all four builds, notes, and a separate local commit.
- Verdict: **SLICE 4.4D READY FOR DATABASE VERIFICATION**. Slice 4.6 remains blocked until 4.4D Stage 2 closes.

## 2026-08-26 — Slice 4.5 Overture Nearby foundation complete locally

- Closed the independent Slice 4.5 Stage 2 gate after 4.5A activation. The bounded retained `2026-08-19.0` run used only three confirmed public canonical UAT venues and no household/private origins: 3 boxes, 16,764,451 bytes, 819 candidates examined, about 13 seconds, concurrency 1, and dry-run before apply. Final V2 operating thresholds are the tunable 3-mile pre-filter, 15 candidates per venue/category, `candidate-quality-v2`, and `dedupe-v2`; the reviewed active pool remains 87 rows (45 Food/42 Coffee), with the prior 51 legacy rows inactive.
- Fixed the aggregate quality report's venue-level denominator to use latest successfully activated refresh scopes, including empty pools, rather than only event-associated identities; canonical event weighting now reads `venue_id`. The live report now correctly returns 3 enriched venues, 100% Food/Coffee/quick-option fill, Food pools `0 zero / 0 partial / 3 full`, Coffee pools `0 / 1 / 2`, zero duplicate rate, and 87 active candidates. Event-weighted Food coverage remains honestly 0% because the associated-event denominator is zero. Historical refresh success/failure is 2/1 because one empty failed diagnostic audit record is intentionally retained without scopes or child rows.
- Strengthened the catalog verifier to prove the unique evidence observation identity plus atomic upsert concurrency contract. Strengthened the rollback-only behavioral verifier to cover repeat-writer idempotency, active/merged/reconciled/suppressed resolver semantics, V2-classified atomic activation, failed-refresh preservation, provenance, and Foursquare rejection. The updated catalog verifier passed; the behavioral verifier returned `SLICE 4.5 BEHAVIORAL VERIFICATION PASSED; ROLLBACK CLEANUP ZERO`. Two failed fixture-only iterations (duplicate created before eligibility, then missing mandatory 4.5A classification) rolled back cleanly before the corrected pass.
- Independent final reads returned zero exact verifier provisional IDs, zero provisional venues/evidence, zero staging refreshes, zero fixture candidates, and zero fixture refreshes; the real active pool was unchanged. Final validation passed 155 Corralio tests, both TypeScript checks, zero-warning Corralio lint, `git diff --check`, and all four production builds; RI/TI emitted only existing warnings. Final verification made zero provider/download, geocoding, routing, canonical/provisional retained-write, promotion, cron, push, or deployment usage.
- Verdict: **SLICE 4.5 COMPLETE LOCALLY**. Slice 4.5A remains separately **READY FOR 4.6**.

## 2026-08-26 — Slice 4.5A physical-identity hardening and atomic replacement complete

- Added and locally committed the generalized V2 physical-place identity hardening as `4275920a` (`Harden Corralio Overture physical identities`). Deterministic address normalization plus 150-meter coordinate proximity now finds compatible same-place identities without deduplicating by brand alone. Same-identity duplicates select a stable evidence winner; materially different identities at the same physical place use confirmed operating status and a versioned permitted-source authority order, while unresolved collisions fail closed. Unconfirmed sub-0.80 identities are excluded generically. Exact `2026-08-19.0` Dwight Merkel regression fixtures cover Fast Freddies, Mofro/Dutch Bros, White Dog/Jitterz, Wellesley Starbucks, and the distinct Indian Trail Starbucks addresses.
- A human applied both reviewed 4.5A migrations to the configured Supabase target in order: candidate-quality/food-tag V1 followed by physical-identity V2. The V2 migration was repaired to discover any prior quality/dedupe CHECK names and now has an explicit V1 prerequisite guard. The read-only catalog verifier returned `SLICE 4.5A CATALOG VERIFICATION PASSED`; the rollback-only behavioral verifier returned `SLICE 4.5A BEHAVIORAL VERIFICATION PASSED; ROLLBACK CLEANUP ZERO`. This proves the applied V2 defaults/checks, hardened activation contract, typed candidate/tag coherence, atomic failure preservation, and zero retained verifier fixtures.
- The user then explicitly authorized the production candidate-pool write. A read-only preflight proved the active production scope was exactly the original three canonical venues and six Food/Coffee scopes: UWM Sports Complex, Evolution Sportsplex, and Oakland University Recreation Outdoor Complex, with 51 active `legacy-v0` rows. Dwight Merkel was not an active scope and remained regression-only. The retained bounded extract was regenerated and dry-run first with the same release and bounds: 3 venues, 3 boxes, 16,764,451 bytes, 819 candidates examined, about 13 seconds, concurrency 1, 87 selected, 45 Food, 42 Coffee, five physical-identity collision exclusions, and zero unresolved identities.
- The reviewed three-venue pool was then applied through the service-only atomic refresh path. Post-activation reads independently confirmed all 87 new candidates active with `candidate-quality-v2` and `dedupe-v2`, all 51 prior candidates inactive with null activation timestamps, six active scopes at or below the 15-row cap, zero confirmed-closed active candidates, zero staging refreshes, zero candidates missing provenance, 252 normalized provenance rows, and 31 correctly versioned food-tag rows. The active scope counts are UWM 15 Food/15 Coffee, Evolution 15/15, and Oakland 15/12. The selected pool retains 20 quick-service, 5 pizza, 8 sandwich, 3 brewery, 9 other-Food, and 42 Coffee candidates; status is 78 confirmed open and 9 unknown.
- The first apply attempt failed closed before creating a refresh because the sandbox could not resolve the Supabase host; the legacy pool remained active and unchanged. A zero-candidate diagnostic refresh was explicitly marked failed to isolate table-write permissions and contains no venue scope, candidate, provenance, or tag data. The authorized unsandboxed retry completed atomically. No new Overture download, routing call, geocoding call, household/private-location access, canonical/provisional venue write, promotion, UI change, cron, push, or deployment occurred.
- Final validation passed all 151 Corralio library tests, `git diff --check`, and production builds for all four workspaces (`corp-app`, `corralio-app`, `referee-app`, and `ti-web`); RI/TI emitted only their existing warnings. Database usage was one successful bounded refresh (6 scopes, 87 candidates, 252 provenance rows, and 31 food-tag rows), one empty failed diagnostic audit row, and atomic deactivation of the prior 51-row pool. Provider usage remained zero because the reviewed retained extract was reused.
- Verdict: **SLICE 4.5A READY FOR 4.6**. Slice 4.5's separate Stage 2 completion gate remains independent and must be satisfied before enabling 4.6 runtime behavior.

## 2026-08-26 — Slice 4.5A structured food-tag metadata patch

- Merged the approved pre-migration food-tag correction into the canonical 4.5A prompt. The exact versioned `food_tags` vocabulary is Mexican, Chinese, Italian, Japanese, Sushi, American, Burgers, and BBQ. Tags derive only after candidate acceptance from audited structured taxonomy/category primary, hierarchy, and alternate values; names, brands, free text, unsupported categories, and rejected candidates cannot produce or rescue tags.
- Candidate behavior is intentionally unchanged: tags do not participate in acceptance, rejection, deduplication, winner selection, diversity, ranking, or the 15 Food / 15 Coffee caps. The same retained `2026-08-19.0` replay still selected exactly 87 rows (45 Food and 42 Coffee) with the same intent/status/rejection distributions. It produced 31 tag occurrences on 22 Food candidates; 23 accepted Food candidates remained correctly untagged. Aggregate tags were American 11, Burgers 9, Italian 5, Mexican 3, Chinese 1, Japanese 1, Sushi 1, and BBQ 0 in the capped pool.
- Extended the still-unapplied 4.5A migration with a normalized service-only food-tag table: exact value/evidence/rule constraints, unique candidate/tag identity, cascade cleanup, composite provenance ownership integrity, PostgreSQL ownership, forced RLS, and service-role-only select/insert. Each tag records its structured evidence field and an actual permitted normalized provenance row; exact field provenance wins, with an audited record-level null-property source as the deterministic fallback. No raw payload, URL, JSONB, private/household content, invented property path, or ad hoc legacy backfill is stored.
- Atomic refresh now stages tag rows beside their candidate and activates through the unchanged parent pool. A hardened trigger rejects active-pool mutation, Coffee/tag incoherence, wrong-candidate provenance, and fabricated/mismatched evidence properties before activation; any failed refresh preserves the prior active candidate/tag state. The catalog and rollback-only behavioral verifiers now cover the tag table, vocabulary, FKs/cascade, provenance ownership, grants/RLS/ownership, optional no-tag candidates, invalid values, staging/activation coherence, failure preservation, and rollback cleanup. Stage 2 remains gated on human migration application followed by both SQL verifiers and reviewed atomic replacement. No migration, database write, provider call, push, or deployment occurred in this patch.
- Verification passed 148 Corralio tests, the focused 26-test 4.5/4.5A suite, Corralio and standalone operations/report TypeScript, zero-warning Corralio lint, `git diff --check`, and production builds for `corp-app`, `corralio-app`, `referee-app`, and `ti-web`. Referee/TI produced only their pre-existing warnings. Usage was zero new Overture downloads or other provider calls, database mutations, backfills, cron executions, pushes, and deployments; all UAT analysis replayed the retained local payload.

## 2026-08-25 — Slice 4.5A Overture candidate quality hardening Stage 1

- Added the canonical 4.5A prompt with the authoritative quality corrections and final audit safeguards: no exact-name/feature blacklist, exact pool/intent semantics, semantic replay idempotency, split TypeScript/SQL verification responsibilities, and a stop gate if generalized deterministic evidence cannot preserve legitimate local businesses.
- The versioned candidate rule now combines permitted provenance, structured taxonomy, generalized contradictory-identity checks, explicit operating status, address/identity completeness, and existence confidence. It rejects confirmed closure, bar/liquor/nightclub leakage, healthcare, real-estate, senior-care, government, mixed liquor/gas, legal-entity display names, weak addressless identity, and uncertain low-confidence breweries. Brand recognition is not required. The deduper uses same feature identity or exact address plus alias-equivalent name within 150 meters, with a stable provenance/status/confidence/completeness/ID winner.
- Preserved the existing `food | coffee` atomic scopes and 15-row broad-category caps. Prepared an unapplied migration adding exact constrained `intent_category`, three-state `operating_status`, and quality/dedupe rule versions with deploy-safe legacy backfill. Transitional active rows are honestly marked `legacy-v0`; only later inserts default to V1, and activation accepts only explicitly V1-classified rows. The replacement activation RPC rejects closed, incoherent, unversioned, provenance-incomplete, scope-mismatched, and over-cap staged pools. No canonical, provisional, lifecycle/evidence, promotion, or authenticated-access boundary changes were introduced.
- The audited property-scoped `overture-signals` path is accepted only for `/properties/operating_status` with an explicitly declared `CDLA-Permissive-2.0` license; missing/mismatched declarations fail closed. All records bearing Foursquare provenance remain excluded. Catalog and rollback-only database verifiers were prepared, while pure classification/deduplication/diversity behavior stays in TypeScript tests.
- Replayed the same retained release `2026-08-19.0` extraction in dry-run only: 3 public venues, 3 boxes, 16,764,451 bytes, 6,713 unique raw features, 819 relevant venue-scope candidates, about 13 seconds, concurrency 1, and zero database/routing/geocoding mutation. Results moved from 51 to 87 selected rows while retaining 45 Food and expanding Coffee from 6 to 42. Intent distribution is 20 quick service, 5 pizza, 8 sandwiches, 3 brewery, 9 other Food, and 42 Coffee; status is 78 confirmed open and 9 unknown. All 10 confirmed-invalid baseline rows and both original questionable rows are excluded by generalized rules; three newly selected café identities remain explicitly review-needed. Confirmed contamination fell from 19.61% to 0%, Food/quick/Coffee availability all remained 3/3, and no confirmed-closed row survived.
- Stage 1 does not apply the migration or replace the currently active 51-row pool. Required Stage 2 order is human migration apply, catalog verifier, rollback-only behavioral verifier, bounded dry-run, reviewed atomic replacement, immutability/provenance/failed-refresh verification, and cleanup zero. Full evidence and commands are in `docs/reports/corralio-slice45a-stage1-audit-2026-08-25.md`. No push, deployment, cron, canonical/provisional write, promotion, or provider call occurred. Verdict: **SLICE 4.5A STAGE 1 COMPLETE LOCALLY; MIGRATION AND ATOMIC REPLACEMENT UAT REQUIRED**.
- Local verification passed 144 Corralio tests, the focused 22-test 4.5/4.5A suite (including order-independent semantic replay), Corralio TypeScript, standalone report TypeScript, zero-warning Corralio lint, `git diff --check`, and production builds for all four workspaces (`corp-app`, `corralio-app`, `referee-app`, and `ti-web`). Referee/TI emitted only their existing warnings. Usage was zero new Overture downloads or other provider calls, database mutations, backfills, cron runs, pushes, and deployments; the dry-run replayed the retained bounded payload. SQL verifiers remain intentionally unrun until a human applies the migration.

## 2026-08-25 — Slice 4.4C provisional lifecycle and evidence complete locally

- After the base migration and additive named-conflict-target repair were applied, the repair sentinel, strengthened owner/catalog verifier, and rollback-only behavioral verifier all passed, including exact forced-RLS/grant/function contracts, evidence idempotency and source independence, generic-ICS ineligibility, unsupported evidence rejection, merge authority/idempotency/one-hop redirects, forced-transition atomicity, suppression, existing-canonical reconciliation without canonical or event-geocode mutation, role denials, anonymized history retention, and rollback cleanup zero. Database concurrency safety is enforced by the verified unique observation constraint plus named-constraint `ON CONFLICT`; a retained two-session tombstone was not manufactured because service cleanup is intentionally prohibited.
- The aggregate quality report returned the correct clean pre-launch state: zero geocoded ICS events, eligible locations, associations, lifecycle rows, duplicates, observations, strong evidence, and promotion-eligible provisional venues. Independent reads found zero external API-call rows, daily quota rows/reservations, provisional venues, evidence rows, and transition rows.
- Claude's independent signed-in local browser regression passed This Weekend, Family, and Upcoming. Four schedules and the existing conflict rendered correctly; estimated leave-by was correctly absent because the household origin is empty; family/team, private-origin, calendar-link, directions, and placeholder behavior remained intact; no provisional/evidence UI leaked; and no application console error, exception, failed UI state, or overlay appeared. No credentials were entered.
- Stage 1's 168 tests, both TypeScript checks, zero-warning Corralio lint, diff checks, and all four production builds remain the final code-validation baseline. Stage 2 made zero provider/source-feed calls, canonical writes, backfills, crons, pushes, or deployments and retained no fixture. Quick-check remains non-strong, production evidence remains ICS-only, and zero eligible venues remains correct until an audited strong source exists. Verdict: **SLICE 4.4C COMPLETE LOCALLY**.

## 2026-08-25 — Slice 4.4C provisional lifecycle and evidence Stage 1 ready

- The user applied the 4.4C migration and the additive evidence-conflict repair. The repair returned `SLICE 4.4C EVIDENCE CONFLICT REPAIR PASSED`; the strengthened catalog verifier returned `SLICE 4.4C CATALOG VERIFICATION PASSED`; and the rollback-only behavioral verifier returned `SLICE 4.4C BEHAVIORAL VERIFICATION PASSED; ROLLBACK CLEANUP ZERO`. This completes the migration/catalog/rollback SQL portion of Stage 2 without retained verifier fixtures.
- The first behavioral-verifier run found a PL/pgSQL ambiguity between the V2 RPC's `provisional_venue_id` output variable and the evidence column in a column-list `ON CONFLICT` target. The base migration now targets the named unique constraint, and `20260825_corralio_slice44c_provisional_lifecycle_evidence_fix.sql` repairs an already-applied migration without data changes. Catalog and architecture checks now guard the exact fix.
- The repository audit verdict is `QUICK_CHECK_NOT_STRONG_ENOUGH`: TI's quick-check is anonymous/browser-scoped and subjective, so production 4.4C supports only low-trust `ics_observation`, exposes no strong-evidence writer, and correctly produces zero promotion-eligible provisional venues. Future strong-evidence behavior is covered only at the pure deterministic rule boundary.
- Prepared, but did not apply, `20260825_corralio_slice44c_provisional_lifecycle_evidence.sql`. It adds coherent `active`/`suppressed`/`merged`/`reconciled` lifecycle state, immutable transition history, service-only exact and trusted merge paths, one-hop redirects, suppression, and reconciliation to existing read-only canonical truth without creating or modifying canonical venues. The V2 create/reuse RPC is canonical-first and handles every lifecycle state.
- Added typed, bounded, idempotent evidence with versioned keyed HMAC source-scope/observation fingerprints. Evidence stores no raw URL, credential, private source/household/event ID, location, note, or arbitrary payload; anonymized evidence intentionally survives source deletion. The dedicated server-only key has no unsafe fallback. Generic ICS volume and distinct-source counts remain non-strong.
- Prepared machine-failing catalog and rollback-only behavioral verifiers plus an aggregate-only quality report. Stage 1 passed 168 Corralio tests, both TypeScript checks, zero-warning Corralio lint, `git diff --check`, and production builds for all four workspaces; RI/TI warnings remain pre-existing. Usage was zero provider/source-feed calls, database mutations, backfills, cron, canonical writes, pushes, or deployments. Canonical ADR/roadmap files were not touched because they contain unrelated changes; exact proposed amendments are recorded in the Stage 1 report. Verdict: **SLICE 4.4C READY AFTER MIGRATION AND STAGE 2 VERIFICATION**.

## 2026-08-25 — Slice 4.4B shared provisional venues complete locally

- After the user applied `20260825_corralio_slice44b_shared_provisional_venues.sql`, the owner-level catalog verifier passed the exact table owner, forced-RLS/no-policy boundary, grants, typed association and coherence constraint, invoker/fixed-search-path RPC contracts, and unchanged public venue surface. The rollback-only behavioral verifier passed manual-event rejection, cross-household reuse, suppression/detachment, authenticated denial, and its independent post-`ROLLBACK` cleanup-zero assertion.
- Live role probes independently confirmed anon and authenticated denial for both the provisional table and creation RPC, service-role table access, and service-role execution through a no-write invalid request. The one synthetic Auth probe was deleted and independently absent. A provider-free canonical-conflict fixture used one read-only canonical candidate, returned `canonical_conflict`, created no provisional row, and cascade-cleaned its household/source/event/match rows to zero.
- A literal two-session-equivalent HTTP concurrency UAT invoked the transactional RPC simultaneously for two synthetic households. The outcomes were exactly one `created` and one `reused`; both private associations referenced the same single shared identity. Suppression detached both associations, private fixture cleanup returned zero, and the user deleted the exact suppressed fixture tombstone with owner SQL. An independent service-role check then returned zero for the fixed identity key, the entire provisional table, and `corralio_external_api_calls`.
- The aggregate Venue Identity Coverage report returned zero geocoded ICS events, eligible locations, canonical/provisional associations, unresolved rows, active provisional rows, potential duplicates, and zero-association rows, consistent with the clean pre-launch dataset. Local browser regression returned 200, meaningful desktop content and expected controls, with no framework overlay; its screenshot/session and the dev server were removed. Stage 2 passed 156 current Corralio tests, TypeScript, zero-warning Corralio lint, `git diff --check`, and production builds for `corralio-app`, `corp-app`, `referee-app`, and `ti-web`; RI/TI warnings remain pre-existing and unrelated.
- Usage/cost impact for Stage 2 was zero provider requests and zero retained `corralio_external_api_calls`; no source feed, Geocodio, OpenRouteService, Mapbox, Overture, Nearby, backfill, cron, canonical/public venue write, push, or deployment occurred. The feature remains bounded post-persistence, service-only provisional intelligence with canonical-first reconciliation and durable production suppression semantics. Exact ADR amendments remain deferred because the canonical ADR file contains unrelated uncommitted work. Verdict: **SLICE 4.4B COMPLETE LOCALLY**.

## 2026-08-25 — Slice 4.4B shared provisional venues Stage 1 ready

- Implemented the final product decision that generic successfully persisted ICS events may contribute low-trust preliminary place evidence without Slice 4.4B.1; manual events remain categorically ineligible. The repaired v3 prompt defines the durable pipeline as low-trust ICS location → structurally isolated provisional identity → later Overture corroboration → separately validated canonical/public promotion.
- Prepared, but did not apply, `20260825_corralio_slice44b_shared_provisional_venues.sql`. Its shared table is separate from `public.venues`, forced-RLS and service-role-only, and contains only bounded parsed place identity, coordinates, lifecycle/provenance tokens, and timestamps—never household/event/source IDs, complete raw event text, URLs, credentials, notes, child/team data, or routing data. The existing private event-match row gains a database-enforced mutually exclusive provisional state and retains cascade deletion with its event.
- Added conservative named-place parsing with no sports keyword allowlist, while rejecting household-origin results upstream plus bare addresses, home/away/TBD values, logistics, street-only names, and orphan field/court labels. Creation reuses the existing successful Slice 4.3 geocode and adds zero provider calls. Location changes clear the prior association through the existing matcher fingerprint path; due re-evaluation remains canonical-first and atomically clears provisional identity when canonical truth wins.
- Added a service-only invoker RPC with fixed search path, exact ICS/geocode/match validation, an in-transaction canonical race check, complete provisional candidate checks, deterministic name/locality advisory locking, idempotent create/reuse, and a durable suppression tombstone. The post-geocode hook is bounded to 200 event IDs and separately caught, so failure cannot affect ingestion, source health, routes, leave-by, rendering, or family events.
- Prepared the machine-failing catalog verifier, rollback-only behavioral verifier, and aggregate-only Venue Identity Coverage/duplicate/orphan report. Stage 1 passed 205 Corralio/shared regressions, Corralio TypeScript and zero-warning lint, `git diff --check`, and all four production builds; RI/TI emitted only their existing unrelated warnings. Usage review: zero external calls, zero database mutations, zero cron/scheduled work, and no backfill, deployment, push, or canonical/public venue write. Exact ADR-008/030/033 amendments are proposed in the Stage 1 report without touching unrelated uncommitted canonical-doc work. Stage 2 remains gated on human migration application and database/browser/concurrency/cleanup-zero verification. Verdict: **SLICE 4.4B READY AFTER MIGRATION AND STAGE 2 VERIFICATION**.

## 2026-08-25 — Slice 4.4B provisional venue foundation audit stopped at source trust

- Merged the reviewed 4.4B specification into `docs/prompts/corralio-slice-4.4b-provisional-venue-foundation.md`, including dedicated service-only provisional storage, typed canonical/provisional identity, privacy and facility-name gates, truthful persisted Geocodio provenance, atomic concurrency, strict RLS/RPC requirements, zero incremental provider calls, coverage reporting, production-disable/backfill gates, two-stage verification, and cleanup-zero requirements.
- The mandatory audit proved that Corralio has no trustworthy schedule-provider classification to authorize shared venue creation. `corralio_schedule_sources.source_type` is fixed to generic `ics`; display name and sport are user-controlled; the protected URL may be credential-bearing; the generic server fetcher establishes only SSRF-safe HTTP(S) retrieval and valid ICS content; and the normalizer emits no publisher identity. ADR-019 deliberately defers direct TeamSnap, GameChanger, and SportsEngine integrations. Trust therefore cannot be derived from current non-secret persisted metadata without inventing a policy forbidden by the prompt.
- Stopped before schema or runtime implementation and recorded the smallest prerequisite in `docs/reports/corralio-slice-4.4b-source-classification-audit-2026-08-25.md`: a narrow, server-derived, versioned provider/source classifier that defaults to `unclassified`, cannot be client-written, clears on URL replacement, does not auto-backfill historical sources, and requires separately approved exact trust evidence. Existing generic ICS planning remains unchanged and usable. The documentation-only audit passed `git diff --check`; application tests and builds were not rerun because the hard gate prohibited all schema/runtime changes. No provisional/canonical venue or shared evidence row, migration, verifier, external call, database mutation, ADR change, cron, push, deployment, backfill, or production enablement occurred. Verdict: **SOURCE CLASSIFICATION PREREQUISITE NOT MET — SLICE 4.4B NOT READY**.

## 2026-08-25 — Slice 4.4 location foundation (complete locally)

- The prerequisite audit confirmed the completed Slice 4.3 event geocode/route foundation, its location-change invalidation trigger, the authenticated/service-role `venues_public` read boundary, and TI's tested `venueResolution.ts` behavior. The port remains Corralio-local rather than creating a cross-app import. It deliberately removes TI's capped global `.limit(5000)` fallbacks: a candidate set must be complete before either uniqueness or authoritative unmatched status is inferred. Corralio therefore uses deterministic pagination over complete state scopes and filters normalized city in memory; locations without a safe city/state scope remain `insufficient_location`. `venues_public` still lacks coordinates, but the preferred relationship-only model does not consume them, so no shared-view or canonical venue change was made. No current manual-event mutation path exists; matching covers initial schedule connection, validated replacement, and scheduled refresh.
- Prepared `20260825_corralio_slice44_location_foundation.sql` for human application; the user later confirmed it applied. The service-role-only, forced-RLS `corralio_event_venue_matches` table stores one household-safe row per event with an advisory no-FK canonical venue UUID, status, salted SHA-256 location fingerprint, matcher version, evaluation/match timestamps, and unmatched recheck time. Composite event/household identity, cascade cleanup, 64-character fingerprints, allowed states, venue/timestamp coherence, and 30-day unmatched rechecks are database-enforced. It stores no raw location, canonical venue attributes, coordinates, route data, or client-write surface. Missing canonical rows make a match eligible for re-evaluation without affecting the event.
- The V1 privacy normalizer handles case, punctuation, Unicode apostrophes, directional/street/unit aliases, ZIP/country suffixes, and parsed unit/sub-location suffixes. Exact normalized complete/base equality classifies the household origin before any venue query; loose substring matching is prohibited, so `12 Main` cannot match `112 Main`. Private and insufficient events keep their Slice 4.3 geocode and record only bounded provenance. Matching never overwrites `location_lat`, `location_lng`, geocode provenance, route results, or leave-by state.
- Matching is a separately caught post-persistence phase. Matcher/query/decoding/upsert failures cannot reverse an interactive connection success, mark a scheduled source failed, increment refresh failures, or roll back schedule data; failure logs are one constant payload-free line. Unchanged current matched/private/insufficient outcomes reuse. Unmatched outcomes reuse only until their 30-day `recheck_after`; fingerprint/version changes, missing venues, and the internal trusted-server `forceRematch` option re-evaluate immediately. Candidate work is grouped by normalized city/state, each complete state scope is reused and paginated in 500-row pages, and event/existing/current-venue reads use bounded 100-ID batches. Completion logs only aggregate counts.
- After the user applied the migration, the read-only `corralio_slice44_catalog_verification.sql` passed explicit machine-failing assertions for the exact columns, constraints, indexes, owner, forced-RLS/no-policy boundary, grants, and unchanged no-coordinate `venues_public` contract. The network-free, rollback-only `corralio_slice44_behavioral_verification.sql` passed all four states, household-safe event identity, invalid-state rejection, authenticated denial, cascade behavior, and an independent post-rollback cleanup-zero assertion.
- Disposable database/browser UAT created one confirmed `.invalid` Auth identity, one household, one inert and unfetched `.invalid` source, and three weekend events. A real read-only canonical venue candidate produced exactly one `matched`, the exact normalized household address produced one `private_skipped`, and a synthetic non-candidate address produced one `unmatched`. Provenance persisted correctly; all three Slice 4.3 event coordinate pairs remained byte-for-byte numerically unchanged. The matched and unmatched cards retained fresh estimated leave-by data; the same-origin private card remained usable without a route. At desktop and 390px mobile, all three cards rendered, estimated copy appeared only twice, the existing three-choice navigation dialog worked, no match internals or new venue UI appeared, and there was no overflow, framework overlay, console error, or page error.
- The post-render usage check remained exactly zero `corralio_external_api_calls`: no Geocodio, OpenRouteService, source-feed, or other external request was made. Cleanup deleted the exact household and Auth identity, cascade-removed membership/source/event/match/weekly-engagement/quota data, and independently returned zero across every fixture-owned table and Auth. No existing row or canonical venue was written; the canonical venue surface was read-only. Credentials, fixture addresses/coordinates, sensitive IDs, screenshots, ledger, and harness were removed and were not retained in the repository or notes. Stage 1 validation remains 158 tests, TypeScript, lint, diff checks, and all four production builds; Stage 2 changed only verifier assertions and notes. Usage impact remains database-only post-persistence work with no new Vercel function, cron, render-time provider call, provisional/canonical venue creation, Overture, Nearby, UI, or routing behavior. No push, deployment, or cron occurred. Verdict: **SLICE 4.4 COMPLETE LOCALLY**.

## 2026-08-25 — Slice 4.3 estimated leave-by (complete locally)

- The user reported the reviewed migration applied and both server-only provider keys configured for local/server use. The read-only catalog verifier initially exposed a verifier-only `ORDER BY` alias-cast error; after sorting by the underlying catalog expression and querying `pg_catalog.pg_trigger` directly so trigger visibility is role-independent, all 18 nullable/no-default application columns, expected CHECK/FK definitions and delete behaviors, both service-role-only RLS/grant boundaries, the exact location trigger, and all three function owner/security/search-path/grant contracts passed. The rollback-only behavioral verifier then completed through its terminal `ROLLBACK`, covering null defaults, normalization/invalidation, owner-derived origin preparation, deterministic losing claims, exact quota capping, valid audit states, invalid audit/coordinate/partial-route rejection, and fixture isolation without any provider request. An independent read-only check returned zero Auth users, households, memberships, events, API-call rows, and quota rows for every fixed fixture ID.
- Live UAT found two integration defects before provider execution: Geocodio v2 requires request filter `country=USA` while returning response country `US`, and Supabase/PostgREST `.or(...is.null...)` on the claim UPDATE silently matched zero rows. The adapter now uses the documented request vocabulary. Origin and event claims now use two atomic compare-and-set attempts (unclaimed first, stale second), preserving deterministic concurrency exclusion and stale-claim recovery without the broken UPDATE OR filter; a disposable database probe reproduced the old zero-row behavior and proved both replacement predicates.
- The final disposable browser/provider evidence pass used one confirmed `.invalid` Auth identity, one household, one inert unfetched `.invalid` schedule source, two routable manual events, and one deliberately ungeocodable manual event. Raw `createUser` output was suppressed immediately; credentials/email stayed in memory and only the Auth ID entered a mode-`0600` `/tmp` ledger. At 1280×900 and 375×812, two cards rendered labeled `(est.)` leave-by lines with estimated-drive copy, the failure card remained visible without leave-by, and Apple Maps, Google Maps, Waze, and Copy address remained available. There was no horizontal overflow, framework overlay, console/page error, or failed local response. Database checks confirmed persisted private origin coordinates/timestamp, two accepted event geocodes with positive duration/distance and `openrouteservice` freshness metadata, and one definitive geocode failure with no route.
- The passing run's service-role audit ledger independently confirmed exactly 4 billable Geocodio calls (one origin plus three event attempts), 2 billable OpenRouteService calls, 6 total, one non-retryable geocode failure, and no routing failure. Two earlier post-fix disposable runs each reached the same deterministic six-call provider phase before exposing fixture/harness-only issues (a destination effectively colocated with the origin, then a close-control locator expecting `Close` instead of the actual `Cancel`), so the complete authorized session consumed 12 Geocodio and 6 OpenRouteService requests. Earlier claim-skipped attempts were nonbillable and made no provider request. This exceeded the prompt's original eight-call UAT ceiling only after the user explicitly authorized continued testing based on ample daily free quota; no automatic retry loop was added to the product.
- Cleanup deleted the exact provider-audit rows and disposable household, cascade-removed membership/source/event/quota rows, deleted the exact Auth identity, independently confirmed zero fixture rows and Auth absence, and confirmed the established smoke identity unchanged. Temporary screenshots and harnesses were deleted and the local server was stopped. Final validation passed 133 Corralio/shared tests, TypeScript, Corralio lint, `git diff --check`, and production builds for all four workspaces; the usage/cost review matched the passing audit ledger. Existing RI/TI build warnings remain unrelated and non-blocking. No fixture address, coordinate, credential, email, Auth/household/event ID, or provider payload was retained in the repository or notes. No cron, production data mutation beyond the user-applied schema, push, deployment, or vendor-dashboard action occurred. Traffic-aware routing, per-event/multiple origins, notifications, venue matching/POIs, mapping UI, and dashboards remain deferred. Verdict: **SLICE 4.3 COMPLETE LOCALLY**.

## 2026-08-25 — Slice 4.3 estimated leave-by (Stage 1 ready)

- Prerequisite commit `3574ce14` and the `SLICE 4.2A COMPLETE LOCALLY` baseline were confirmed. The audit found no Corralio home coordinates, event geocodes, routing cache, Geocodio integration, or shared Corralio external-call ledger. Household settings already live on Family; `This Weekend` already preserves raw schedule location and its Apple Maps/Google Maps/Waze/copy chooser. The RI Mapbox geocoder and `external_api_calls`/tracking helper remain RI-owned and were not imported or written by Corralio. The repository still has no local/staging Supabase database, so the reviewed migration remains unapplied for a human-controlled Stage 2.
- Added one optional private origin per household, never per event and permanently prohibited from venue evidence, canonical matching, or cross-household reuse. Geocodio is the new server-only geocoder because its storage/economics fit geocode-once persistence; acceptance requires accuracy >= 0.8 AND `rooftop`/`point`/`range_interpolation` AND valid coordinates AND US country. OpenRouteService on the HeiGIT host is the isolated non-traffic `driving-car` routing adapter; Mapbox Directions remains rejected because its Navigation terms conflict with persisted derived routing results. Provider payloads and raw errors are never persisted or logged. The example environment now contains only blank server-side key placeholders; any credential previously copied into that tracked example should be rotated if it was ever shared.
- Prepared unapplied migration `20260825_corralio_slice43_leave_by.sql`. Nullable coordinate pairs, success/failure mutual exclusion, 1–720-minute duration, positive distance, fixed provider/API/status/error vocabularies, and audit-state coherence are enforced with CHECK constraints. Audit rows retain their independent record if a household is deleted (`ON DELETE SET NULL`); daily quota rows cascade with the household. Both new tables are RLS-enabled and service-role-only. Authenticated event writes are narrowed to the established manual fields so clients cannot tamper with computed data. A location trigger normalizes raw event locations and invalidates all geocode/route/claim state. The owner-derived origin RPC locks the household, changes or clears the origin atomically, preserves destination geocodes, and invalidates all household routes only when the origin changes.
- Geocode and route work is lazy from a real `This Weekend` mount, intersects client event IDs with the session-derived household, active sources, and current server window, and keeps location cards available on every failure. Same normalized locations reuse successful or definitive results across retained household history and share one lowest-UUID representative claim; claim precedes atomic quota reservation, and stale provider responses are claim/address-bound so they cannot overwrite a newer origin. Retryable network/timeout/429/5xx/malformed-provider failures clear claims for a later attempt; definitive no-result/low-quality/invalid geocodes and no-route outcomes set terminal markers. Route rendering is gated against both origin and destination geocode timestamps. The V1 result is visibly labeled estimated and computes event start minus route duration minus the single 30-minute arrival buffer; 30 is tighter, not more conservative, than the founder's 45-minute mockup and remains a founder-confirmable parameter.
- Cost controls are 10 new event geocodes per planner mount, 10 new routes per planner mount, and at most one origin geocode per settings save, with a separate atomic 50-call UTC-day household ceiling. One billable audit row represents one vendor request/quota unit; reuse, intra-batch duplication, lost claims, and daily-cap blocks use payload-free nonbillable skip rows. The read-only usage report covers origin/event outcomes, seven-day provider/error/billable volume, reuse/dedup/concurrency/cap skips, and households nearest the quota. Stage 1 made zero Geocodio and zero routing calls, so realized reuse savings and the real-call counts remain Stage 2 measurements.
- Added offline provider/parser/arithmetic/freshness/dedup/cap/concurrency/audit tests plus static architecture/security coverage, a read-only catalog verifier, and a rollback-only network-free behavioral verifier. Validation passed 133 Corralio/shared schedule tests, Corralio TypeScript and lint, `git diff --check`, React/Next.js review, and production builds for all four workspaces (`corp-app`, `corralio-app`, `referee-app`, `ti-web`). Existing RI/TI build warnings remain non-blocking and unrelated. Stage 2 requires a human to apply the migration, configure both Corralio server-only provider keys, run both SQL verifiers, and complete the capped disposable-fixture real-provider/browser UAT and cleanup. Traffic-aware routing, per-event/multiple origins, notifications, venue matching, POIs, mapping UI, and dashboards remain deferred. No migration, database mutation, real provider call, cron, push, or deployment occurred. Verdict: **SLICE 4.3 READY AFTER LISTED FIXES**.


## 2026-08-24 — Slice 4.2A core usage measurement (complete locally)

- The user manually applied both reviewed Stage 1 migrations in the documented order. Live REST probes confirmed the weekly table and acquisition column, service-role table read, anonymous table/RPC denial (`42501`), service-role RPC execution, and the updated PostgREST function paths. The SQL catalog run confirmed the two-argument `corralio_ensure_owner_household` and three-argument weekly RPC are `postgres`-owned `SECURITY DEFINER` functions with locked `pg_catalog, public` search paths, no PUBLIC/anon execution, and authenticated/service-role execution; the immutable trigger function is `postgres`-owned with no client execution. The rollback-only verification's first run exposed a fixture-only privilege error: it attempted its admin-table timestamp assertion before resetting from `authenticated`. The script now resets before that read and restores the browser role only for later RPC calls. Its rerun completed every final assertion and reached the terminal `ROLLBACK`, covering explicit validation SQLSTATEs, zero-active-source no-op, UTC week anchoring, first/last timestamps, maximum/OR accumulation, mixed verified-plus-unavailable state, provenance normalization/insert-only behavior, trigger rejection, and exact fixture isolation.
- Authorized UAT used exactly two disposable high-entropy, email-confirmed Auth identities created through `auth.admin.createUser`; the raw responses were suppressed immediately, credentials and synthetic emails remained in process memory, and only each exact Auth ID entered a mode-`0600` `/tmp` ledger. Separate fresh 1280×900 browser contexts exercised direct and invited acquisition. The invited context captured the exact 30-day HttpOnly/Lax cookie with `secure=false` on the actual local HTTP transport and no client-visible cookie. Each identity's first household-creating action was the real Family `connectSchedule` path with the prescribed unsupported `ftp:` URL. Both returned the expected safe protocol message, created no source, and produced zero fixture-domain browser requests; Stage 1's injected-boundary test independently proves the same input reaches neither DNS nor fetch. The direct household stored `direct`; the invited household stored `ti_weekend_planner_opt_in`, proving `cookies()` is readable through `supabaseStore.ts`'s independent `resolveOwnerContext()` path. A direct provenance update was rejected with `23514`.
- Controlled service-role fixtures gave the direct household two active inert sources and two overlapping weekend events. A real `/` navigation rendered the one-conflict state and inserted exactly one current UTC-week row with the verified `true/1/false` values. Hovering the `This Weekend` link without clicking produced no observed prefetch request in this run and, critically, no row or timestamp change. Clicking the link updated `last_viewed_at` while preserving `first_viewed_at` and row cardinality. Adding 198 more controlled candidate rows made the exact 200-row boundary render `Conflict check unavailable`; the same weekly row retained `had_conflict=true` and `max_conflict_count=1` while OR-accumulating `conflict_check_unavailable=true`. The authenticated product flow rendered the expected established states with no console or page error; no retry or visible telemetry state appeared.
- Cleanup first deleted only the two recorded disposable households and independently queried zero remaining household, membership, child, team, source, event, and weekly-engagement rows for every exact fixture household. It then deleted only the two recorded Auth identities, independently confirmed both absent, and confirmed the established smoke identity's ID, email, creation/update/sign-in/ban fields were present and unchanged. The ledger, temporary screenshot, harness, and local production server were removed/stopped. No credential, synthetic email, Auth ID, household ID, event detail, or private source value was written to the repository, notes, screenshots retained on disk, or report output. No feed DNS/HTTP request, production cron, push, or deployment occurred. Slice 4.3 leave-by/directions signals remain deferred to this foundation. Verdict: **SLICE 4.2A COMPLETE LOCALLY**.

## 2026-08-24 — Slice 4.2A core usage measurement (Stage 1 ready)

- Prerequisite commit `25a5ba9e` and the `SLICE 4.2 COMPLETE LOCALLY` notes baseline were confirmed. A read-only applied-schema probe returned `200` for the required `created_at`/assignment/status columns on households, sources, children, and teams, and `42703` for the absent household acquisition column. Activation is therefore derived at report time from current source counts and assignment presence; no activation event was added. `ri_analytics_events` and PostHog remain owned by the existing RI surface rather than becoming undocumented cross-product infrastructure. The established migration record was also re-confirmed: Corralio has no local/staging Supabase configuration, and reviewed migrations are manually applied to the production project by a human between stages.
- Prepared, but did not apply, `20260824_corralio_slice42a_core_usage_measurement.sql`. Its RLS-locked Corralio table stores only one household/week aggregate and is service-role-readable. The owner-derived `SECURITY DEFINER` RPC requires a currently non-disconnected source and otherwise safely no-ops. Verified calls require every field non-null, a nonnegative count, and exact `false/0` or `true/positive` consistency; unavailable calls ignore the other values and write nulls on insert. Same-week calls preserve the first timestamp, advance the last timestamp, OR-accumulate conflict/unavailable observations, and retain the maximum count, so verified and unavailable can coexist. The UTC ISO Monday analytics period is intentionally distinct from the unchanged browser-local Friday-through-exclusive-Monday product window. Rows are retained indefinitely for the household lifetime and cascade-delete with it; no pruning job or redundant deletion path was added.
- Prepared, but did not apply, `20260824_corralio_slice42a_acquisition_provenance.sql`. It adds the default/direct or `ti_weekend_planner_opt_in` household tag, locks it with an immutable-update trigger, and drops/recreates `corralio_ensure_owner_household` because changing `(text)` to `(text,text)` must not leave an ambiguous overload. Invalid/null inputs normalize to `direct`, while only the initial insert consumes the value. Middleware captures the exact `?src=` value in a 30-day HttpOnly/Lax, transport-aware Secure cookie before auth configuration checks. Both independent first-creation paths—`getOwnerContext()` and schedule ingestion's `resolveOwnerContext()`—read the same pure validated helper and pass the RPC parameter; no TI identity/session value, UI, import, or logging was added.
- `This Weekend` now submits the already browser-local conflict result once per real client mount through a ref-guarded effect. A pure, non-`use server`, non-`server-only` orchestrator owns viewer/no-household handling and the RPC boundary; resolved or rejected RPC failures remain non-blocking and emit exactly one constant payload-free server log line. The real viewer resolver already collapses auth/membership failures to null/no-household, so those remain silent no-ops; the rejecting-viewer fake covers only the orchestrator catch path. The installed Next.js 14.2.35 boundary and source were reviewed: `/` remains force-dynamic, there is no `loading.tsx`, every family/schedule mutation still revalidates `/`, and `ThisWeekend` still mounts only with a source. Hover/prefetch row-count behavior and `cookies()` in the nested schedule-store request path require applied-schema browser UAT and remain Stage 2 checks, not claims inferred from framework internals.
- Added the current-activation/two-completed-UTC-week retention/conflict-overlap report, read-only catalog verification, and rollback-only behavioral verification. The latter covers validation raises, zero-active-source no-op, mixed accumulation, UTC week anchoring, insert-only provenance, trigger immutability, exact synthetic isolation, and ends in `ROLLBACK`; neither verification script was run because the schema is unapplied. Offline validation passed all 124 Corralio/shared schedule tests, including exact middleware attributes and an `ftp://slice42a.invalid/...` proof that neither injected DNS nor fetch boundary is called, plus TypeScript, lint, and the Next production build. Stage 2 remains explicitly gated on a human applying the weekly migration and then the acquisition migration (the files have no schema dependency, but this is the reviewed operational order), running both SQL verifications, and completing the authorized two-identity browser/DB UAT with raw `createUser` responses suppressed, exact household/Auth cleanup, independent absence checks, and smoke-identity preservation. Slice 4.3 leave-by/directions measurement remains deferred to this foundation. No migration, feed DNS/HTTP, cron, push, or deployment occurred. Verdict: **SLICE 4.2A READY AFTER LISTED FIXES**.

## 2026-08-24 — Slice 4.2 basic family conflict detection (complete locally)

- Repository evidence confirmed `f6fc4e0d` and `d5efbef6` are ancestral and both Corralio notes contain `SLICE 4.1B COMPLETE LOCALLY`. The audit found no reusable TI/RI temporal-conflict engine and no schema need: authorized active candidates already include manual rows plus imported rows from non-disconnected sources, are ordered by `starts_at`, and are bounded at 200 before the client applies the exact browser-local Friday-through-exclusive-Monday window. Imported recurrence instances already have stable event IDs and canonical absolute timestamps. `ends_at` is nullable, and the model has no separate all-day flag.
- Added one pure `candidate events → exact browser-local weekend events → unique pairwise conflicts` pipeline. A conflict requires two distinct stable event IDs and positive valid intervals satisfying `A.start < B.end && B.start < A.end`; touching boundaries, missing/invalid ends, and zero/negative durations are excluded without invented duration. Canonical positive midnight intervals participate according to persisted timestamps without guessing all-day metadata. Comparisons use absolute instants while overlap copy uses the browser-local planning timeline. Pair keys order the two stable IDs, reverse/self duplicates are suppressed, pair ordering is deterministic, and the displayed count means unique pairs. Same-child classification uses the active projection’s resolved owning-child UUID—including team-to-child resolution—never labels or colors; IDs remain non-rendered household-private client metadata. Unassigned events retain the existing connected-schedule fallback.
- `This Weekend` now derives its day groups and conflicts from that same exact selection, presents a restrained factual summary with each event/context pair and overlap window, and marks involved cards with text plus an icon so color is not the only signal. Existing identity, sport, time, location, directions, light/dark, and stable event behavior remain intact. A server-provided boolean marks an exactly-200-row candidate result; in that unknown-completeness state, ordinary weekend events remain visible while all definitive conflict counts/badges are suppressed in favor of calm `Conflict check unavailable` copy. The signed-out synthetic preview was explicitly deferred so marketing work did not dominate the slice.
- Required UAT used one approved wholly disposable household/membership with 2 synthetic children, 3 teams, 6 inert `.invalid` sources, and 7 events: six active events across Saturday/Sunday covered one same-child pair, one different-child pair, one assigned/unassigned pair, a manual event, and an unrelated event; a seventh event belonged to a disconnected source and would otherwise overlap. Database preflight proved the disconnected source/event existed but the active projection contained only six events. At 1280×900 and 375×812 in both light and dark modes, Playwright verified exactly three unique pair rows, five involved-card badges, the same-child/multi-conflict treatments, factual overlap times, source fallback, disconnected exclusion, unrelated-event calmness, directions, theme contrast, scanning, no overflow, no private URL, and zero console/page/HTTP errors. Screenshots stayed in `/tmp`.
- During browser setup, an intentionally early click exposed an existing pre-hydration weakness: the password control was a native submit button before React attached `preventDefault`, allowing a local native GET. The control is now inert `type="button"` until hydration, with explicit hydrated click and password-Enter handlers; focused coverage prevents regression. No external destination received the local smoke form data. After UAT, controlled service-role deletion removed only the recorded disposable household; immediate and independent exact-ID checks both returned `0 household / 0 membership / 0 children / 0 teams / 0 sources / 0 events`, while the smoke auth identity remained and returned to its original no-household baseline.
- Final offline validation passed 102 Corralio tests, TypeScript, lint, the production build, `git diff --check`, and React/Next.js boundary review. No migration, real/`.invalid` feed fetch, cron, retained-family mutation, public conflict data, analytics, persistent conflict history, travel feasibility, leave-by, routing/traffic, resolution/recommendations, venue/tournament context, travel/HotelPlanner, collaboration, notification, push, or deployment occurred. Verdict: **SLICE 4.2 COMPLETE LOCALLY**.

## 2026-08-24 — Slice 4.1B family and schedule lifecycle (complete locally)

- The user applied `20260823_corralio_slice41b_family_schedule_lifecycle.sql` after the reviewed Stage 1 commit `f6fc4e0d`. A consolidated read-only catalog audit confirmed all four expected lifecycle/assignment functions, `postgres` ownership, `SECURITY DEFINER`, locked `pg_catalog, public` search paths, authenticated/service-role execution, anonymous denial, RLS, authenticated source-DELETE denial, absence of the source DELETE policy, service-role cleanup access, retained child-name/team-name/team-sport editing, direct child/team `archived_at` denial, private `source_url`, and no event suppression/archive column. The rollback-only Household A/B verification passed disconnect, connected/disconnected cleanup, manual-event preservation, stale/foreign behavior, privilege hardening, ordinary editing, isolation, and forced-failure atomic rollback. Its independent cleanup query returned zero auth users, households, memberships, children, teams, sources, and events.
- The approved smoke identity began with no Corralio household. Browser UAT therefore used one wholly disposable synthetic household and membership containing one child, Team A for the disconnect/team-removal sequence, a second active Team C for the child-removal cascade, team-assigned Sources A/B, direct-child Source C, an unrelated unassigned control source, and one imported weekend event per source. Starting fixture counts were `1 household / 1 membership / 1 child / 2 teams / 4 sources / 4 events`. Every private source string used `https://slice41b-uat.example.invalid/...`; fixture rows were inserted directly through the controlled database boundary and no URL was fetched.
- Playwright replaced the unavailable `agent-browser` CLI. Before mutation, native confirmation dialogs passed at 375×812 light and 1280×900 dark for Disconnect schedule, Remove team, and Remove child; consequence copy, Cancel/Escape, privacy, and no-overflow behavior passed with zero mutation requests. At 1280×900 light, Source B successfully unassigned and reassigned through the existing UI, with source fallback while unassigned and restored `Child · Team` assignment afterward. Disconnecting Source A removed it and its event from active planning while preserving Source B and the control. Removing Team A archived it, removed it from assignment choices, unassigned both the disconnected Source A and connected Source B, kept Source B connected, preserved Team C, and rendered the Source B fallback. Removing the child then archived the child and still-active Team C, unassigned connected Source C, removed archived family choices, kept Sources B/C and the control connected, and rendered their source fallbacks. Final read-only presentation passed at 1280×900 light and 375×812 dark with no horizontal overflow, console/page/response error, retained private URL value, or rendered `.invalid` URL. A strict-locator ambiguity in the first harness stopped after Team A removal; a database state audit proved the exact safe partial boundary, and the second pass performed only the uncompleted child removal before final checks—no lifecycle operation was repeated blindly.
- Post-UAT database verification confirmed the child and both teams archived; Source A disconnected, unassigned, claim-invalidated, and URL/row preserved; Sources B/C connected and unassigned; the control unchanged; all four imported event rows preserved and unassigned; and stable source/event counts still `4 / 4`. Controlled cleanup then deleted only the disposable household as service role, cascading its synthetic membership/family/source/event rows. Both the cleanup result and an independent exact-ID query returned `0 household / 0 membership / 0 child / 0 teams / 0 sources / 0 events`; the smoke auth user remained and a separate authenticated lookup confirmed it returned to its original no-household baseline.
- Stage 1 validation remains green: 89 Corralio tests, TypeScript, lint, `git diff --check`, isolated production build, and React/Next.js review passed. No real retained family data, real feed, synthetic feed, production cron, source URL disclosure, permanent erasure/history/reconnect, manual-event lifecycle, conflict, leave-by, routing/maps, matching, travel/HotelPlanner, collaboration, notification, provider integration, analytics, push, or deployment was performed. Verdict: **SLICE 4.1B COMPLETE LOCALLY**.

## 2026-08-23 — Slice 4.1B family and schedule lifecycle (Stage 1 ready)

- Repository evidence confirmed `85588cee`, `4a7782ae`, and `a99ef055` are ancestral and that the Slice 4.1/4.1A completion verdicts are recorded. The audited schema matches the selected contract: children/teams already have `archived_at`; sources support `disconnected`; imported events cascade on source deletion; authenticated owners currently have direct source `DELETE`; assignment FKs are restrictive; refresh claims exclude disconnected sources; assignment rejects archived targets; and the active event query previously admitted disconnected-source events before its 200-row limit.
- Added the unapplied migration `20260823_corralio_slice41b_family_schedule_lifecycle.sql`. It removes authenticated source hard-delete permission/policy, narrows authenticated child/team UPDATE to established editable fields so `archived_at` cannot bypass cleanup, and adds separate owner-authorized `SECURITY DEFINER` RPCs for non-destructive schedule disconnect, team removal, and child removal. Missing, foreign, already-disconnected, already-archived, and repeated lifecycle targets return the same bounded `false` result. Disconnect preserves source URL, source/event rows, assignment, stable identity, last sync/attempt, and failure history while clearing only active claim token/time. Team removal archives the team and atomically unassigns every connected or disconnected assigned source plus its imported events. Child removal archives the child and all its active teams, then atomically unassigns direct-child and child-team sources—including disconnected sources—and their imported events. Manual events are not rewritten. Service-role source deletion remains available only for controlled cleanup.
- Replaced the existing assignment RPC in the same migration without changing reassignment/unassignment semantics, moving its lock order to active child, active team, active source, then imported events so it cannot deadlock against the new family lifecycle order. All lifecycle writes remain one database transaction and return no source URL or private event data. The UI uses authenticated Server Actions only; no browser service-role or multi-step mutation was introduced.
- Active planning now derives the authorized, non-disconnected source-ID set first and applies a database-side event predicate that includes manual rows or events from those active sources before `starts_at` ordering and the existing 200-row limit. Disconnected events therefore cannot consume the bounded result and no React/client suppression or event archive column was added. Independent child/team reads remain concurrent with the necessary source-to-event dependency. `/family` adds explicit Disconnect schedule, Remove team, and Remove child controls with native accessible confirmation dialogs, accurate non-erasure/unassignment consequences, Cancel, destructive confirmation styling, and unchanged private URL boundaries.
- Added read-only catalog verification plus rollback-only Household A/B lifecycle/atomicity verification and a separate exact-ID zero-row cleanup audit. The scripts use only inert `.invalid` URL strings, perform no fetch or cron invocation, cover connected/disconnected assignment cleanup, manual-event preservation, cross-household/stale behavior, privilege denial, legitimate editing, unrelated controls, and forced-failure rollback. They were authored but not run because the migration was not applied. Offline verification passed 89 Corralio tests, TypeScript, lint, `git diff --check`, and an isolated monorepo-layout production build without disturbing port 3002. The React/Next.js review found no hook, serialization, RSC, accessibility, or material performance issue. A read-only Playwright login reached `/family`, but the approved smoke household has zero children, teams, or schedules, so lifecycle controls could not be populated without forbidden Stage 1 mutation; no form was submitted and no retained data changed.
- Stage 2 remains explicitly gated on separate approval to apply the exact reviewed migration, run catalog and rollback-only database verification, independently confirm zero retained verification rows, create the approved disposable browser fixture, perform mutation UAT, and clean it up. The browser fixture must use one child, Team A for Sources A/B, a second still-active Team C for the child-removal cascade, direct-child Source C, and an unrelated control source/event; all URLs remain `.invalid` and unfetched. Permanent erasure/history/reconnect, account/household deletion, manual-event lifecycle, conflicts, leave-by, routing/maps, TI/venue/tournament matching, travel/HotelPlanner, collaboration, notifications, provider integrations, analytics, push, and deployment remain deferred. Verdict: **SLICE 4.1B READY AFTER LISTED FIXES**.

## 2026-08-23 — Slice 4.1A landing and onboarding clarity (complete locally)

- The pre-slice signed-out `/` baseline presented `Private pilot`, `Know what’s happening this weekend`, one-schedule ingestion copy, and `Sign in to your family planner` before explaining the multi-child/multi-team family outcome. Account creation existed only as secondary helper copy beneath the password/Magic Link form. Family setup led with `iCal/ICS calendar URL` and `Import schedule`; the repository exposed no shipping Privacy, Terms, Contact, or support destination. Repository evidence confirmed the Slice 4.1 implementation at `85588cee` and populated UAT completion at `4a7782ae` were ancestral to the working branch.
- Signed-out `/` now leads with `The planner built for sports families`, `Every kid. Every team. One plan`, concise multi-app family-problem copy, and the explicit distinction `Team apps organize the team. Corralio plans across the family.` A small server-rendered, static Saturday/Sunday example shows synthetic children, teams, times, activities, and general locations in the established Corralio visual language, ending with `We’ve got the weekend figured out.` It imports no household/product-data boundary, performs no query or fetch, stores nothing, exposes no private data, offers no interaction, and deliberately contains no conflict claim because conflict detection remains Slice 4.2.
- `Get Started` is now the primary signed-out action and reaches the existing email field/Magic Link account path; `Sign in` reaches the existing returning-password treatment. The established `shouldCreateUser: true` Magic Link behavior, password sign-in, show/hide, forgot/reset flow, and shared identity remain unchanged. The unsupported `Private pilot` badge was removed rather than replaced with an unverified early-access claim. A restrained `Your family’s plan is private by default` reassurance reflects the existing household/RLS design. No placeholder legal/support link was added, and the existing `noindex, nofollow` metadata remains unchanged.
- Family setup now leads with `Calendar link`, `Connect schedule`, and `Connect team schedule`; secondary help explains that another team app may call it an iCal or ICS subscription link. Connected schedules use `Change assignment` while retaining the precise `Connected`, `Refresh delayed`, `Schedule needs attention`, and `Replace calendar link` lifecycle/recovery wording. Missing-link validation uses parent language, while technical iCal/ICS terms remain where they help identify or correct a subscription link. Source health, replacement, recovery, assignment, ingestion, refresh, and URL secrecy are unchanged.
- Browser verification used local Playwright because the browser skill’s `agent-browser` executable was unavailable. Signed-out `/` passed exact 375×812 and 1280×900 viewports with actual emulated system light/dark preference: the four comprehension concepts and CTA hierarchy were present, Get Started remained in the initial viewport, both anchor destinations worked, focus was 3px, approved brand variants and the preview rendered correctly, `noindex` remained, and no overflow, console/page error, failed response, private URL marker, `Private pilot`, or conflict claim appeared. A read-only established smoke identity then passed authenticated `/` and `/family` at the same four combinations with unchanged shell/weekend heading, parent-language connection help, empty private URL inputs, and no overflow or errors. Its projection contained no connected source, so populated status/replacement controls were not live re-tested or manufactured; no Get Started email was sent and no family, schedule, feed, or retained row was mutated.
- Verification passed four focused 4.1A tests, the complete 79-test Corralio suite, TypeScript, lint, `git diff --check`, and an isolated production build that did not disturb the active UAT server. A React/Next.js review found no new hook, client-boundary, serialization, accessibility, or material performance concern. No About page, marketing navigation, legal placeholder, testimonial/social proof, pricing/Pro, interactive demo, conflict capability, tournament/travel/HotelPlanner work, analytics, auth provider, migration, RPC, backend infrastructure, external font/feed fetch, cron invocation, push, or deployment was added. Verdict: **SLICE 4.1A COMPLETE LOCALLY**.

## 2026-08-23 — Slice 4.1 populated-state UAT (complete locally)

- The dedicated UAT household now exercises every required connected-schedule presentation state without changing Casey/Owls: Avery/Strikers is assigned to its owning child and team, Drew/Wolves is assigned to Drew only with no team, and Emerson/Ice Owls remains connected with no child or team so its events use the connected-schedule fallback. The retained household presented four children and four connected schedules. On the tested Aug 21–23 weekend, Saturday contained the unassigned fallback, team-assigned, and child-only states together, while Sunday contained a second Drew child-only event. The populated minimum therefore passed for at least two distinguishable children, team assignment, child-only assignment, unassigned/source fallback, events on at least two weekend days, navigable locations, and enough simultaneous content to assess scanning and wrapping.
- Claude in Chrome completed the six requested viewport/mode combinations against `http://localhost:3002`: 375×812, 768×900, and 1280×900 in light and dark. Navigation, non-empty chronological day grouping, all three assignment treatments, color-plus-text identity, card wrapping, contrast, and responsive layout passed without clipping or horizontal overflow. Because the extension could not resize its real window, the two constrained widths were exercised through a same-origin iframe harness at exact CSS dimensions; the real desktop window matched the 1280px pass. Because Corralio follows `prefers-color-scheme` and has no theme toggle, its own dark rules were temporarily applied for screenshots and then removed. Neither harness changed application code, server state, or retained data.
- The directions chooser passed at mobile and desktop widths with Apple Maps, Google Maps, Waze, Copy address, and Cancel visible without clipping; Cancel closed it without an external handoff. `/upcoming` retained its intentional Coming soon placeholder and correct navigation state, while `/family` retained connected-schedule cards and assignment controls. Browser inspection found no application console warning/error, failed request, overlay, rendered ICS URL, `secret=` parameter, or `.ics` reference. Anonymous credential-omitted requests redirected from `/family`, `/upcoming`, and `/account`; `/` retained its established public landing behavior. No direct database edit, source deletion/disconnection, private URL disclosure, push, or deployment occurred during the sweep.
- This populated browser evidence closes the sole listed Slice 4.1 acceptance gap recorded on 2026-08-20. Verdict: **SLICE 4.1 COMPLETE LOCALLY**. Slice 4.2 remains responsible for overlap detection, conflict badges/summaries/explanations, family-plan conflict prioritization, and conflict engagement analytics; all other deferrals in the implementation entry remain unchanged.

## 2026-08-21 — Team-scoped calendar connection follow-up (complete locally)

- Corrected the team-card disclosure affordance after browser feedback: the entire team header is now the native open/close summary, the team name is the underlined primary action, sport remains visible, and a rotating chevron communicates state. The header retains a 44px target, visible focus, and native pointer plus Enter/Space behavior; the separate, spatially disconnected `Edit team` target was removed. The follow-up passed eight focused family/team tests, TypeScript, lint, diff checks, and an isolated production build. Read-only populated browser UAT then passed `/family` at 390×844 and 1280×900 in both system modes: two retained team cards rendered correctly, the name and full header opened/closed by pointer, Enter, and Space, the chevron completed its 90-degree transition, the team-calendar form appeared only when expanded, the focus ring was 3px, and no overflow, failed request, overlay, console error, or page error occurred. No form was submitted or retained data changed.
- Added a dedicated `Connect team schedule` form inside each expanded Edit team panel so the private iCal/ICS subscription link can be pasted where the child, team, and sport context is already visible. The URL field is required, uses URL input semantics, is never populated from retained data, and resets after success.
- The new server action accepts only the team ID and submitted URL from the browser. It resolves the authenticated owner household, then reads the exact active household team and derives its child, display name, and sport server-side before calling the existing SSRF-safe fetch, normalization, source-creation, and canonical event-persistence pipeline. The existing source-creation RPC already validates and accepts child/team assignment, so a newly imported source and its events are assigned to the selected team without a migration or duplicate ingestion path.
- A calendar URL that is already connected is rejected with safe guidance to use the existing connected-schedule assignment control; the team form never silently moves an existing source. Paused sources retain the existing replacement-only recovery behavior. Private URLs remain absent from props, rendered values, success/error messages, and logs.
- Verification passed 25 focused ingestion/assignment/source-security/team-connection tests, the full 75-test Corralio suite, TypeScript, lint, `git diff --check`, an isolated production build, and a read-only signed-out/authenticated runtime smoke against the active port-3002 server with no overlay, console/page error, or overflow. The production build ran from a temporary snapshot so it did not disturb the active UAT server. No form was submitted, no real feed was fetched, and no retained family/source/event data, migration, RPC, schema, cron, push, or deployment changed.

## 2026-08-20 — Slice 4.1 family weekend plan (ready after populated-state UAT)

- The pre-slice audit confirmed the 4.0C shell at `a70eab63` remains intact. Authenticated `/` loaded a broad server candidate window, ordered up to 200 events by `starts_at`, then filtered the exact browser-local Friday-through-exclusive-Monday window in the client. Cards were one flat chronological list; event dates and times used the validated event timezone, assignment presentation used active child/team rows with connected-schedule fallback for unassigned events and `Previous assignment unavailable` for missing active assignments, and cards retained source-derived sport, title, location/field, and the directions chooser. Loading and whole-weekend empty states already existed. The Corralio dev command remains `next dev -p 3002`.
- Added a small pure weekend-plan boundary that converts the existing authorized family/source/event rows into bounded serializable identity fields and groups eligible events by the browser-local calendar day of `startsAt`. Only non-empty Friday/Saturday/Sunday sections render; sections and cards are chronological with stable input ordering for equal timestamps. Day headings use the browser-local weekday/date while the displayed event time continues using its valid event timezone. This intentionally preserves the existing mixed-timezone model, so a late-night event can have a day heading that differs from its event-timezone weekday.
- Child-only cards show the child name and the existing child color; team cards show `Child · Team` and the owning child's color. Identity is always present as text, and color is supplementary through the established `forest`, `ocean`, `amber`, `violet`, `rose`, and `teal` semantic variables. Unassigned events use their connected-schedule name, or `Unassigned event` when no source context exists, with neutral styling. Missing historical assignments retain neutral `Previous assignment unavailable` copy. No name or color is copied into retained event/source data.
- The family projection and broad event query now load concurrently, but the selected columns, household scoping, broad candidate window, `starts_at` ordering, and 200-row limit are unchanged. Event IDs/source IDs and persisted assignment semantics are unchanged. Source-derived sport, title, validated-timezone time, authorized location, field fallback, and the existing pointer/keyboard directions dialog remain available. No private `source_url` was added to the projection, props, or visible event presentation.
- The responsive presentation uses semantic day sections and headings, ordered lists, 44px location controls, text-plus-color identity, wrapping for long names/titles/locations, and semantic light/dark child-color variables. The loading state now announces itself with `role="status"`. A React best-practices pass found no hook, Server/Client boundary, serialization, keyboard, dialog, accessibility, or material performance blocker.
- Read-only browser verification used local Playwright because the installed browser skill had no `agent-browser` executable. Signed-out `/` passed at 375×812 light. Authenticated `/`, `/upcoming`, and `/family` passed at 375×812, 768×900, and 1280×900 in both system modes with the exact three-item primary navigation, correct active route, keyboard activation of Upcoming, correct CSS color scheme, and no overflow, error overlay, console error, or page error. The only request-failure signal was an expected aborted Next.js prefetch during deliberate client navigation. The dedicated smoke identity had no household sources or events, so `/` correctly showed the existing connect-one-schedule state rather than a populated plan.
- Populated browser states—including two children, child-only, team, unassigned/source fallback, unavailable assignment, two weekend days, long content, location/field, and the directions dialog—were not mutation-tested. The user's newly added child and accumulating real events were not accessed or changed, and no synthetic fixture was created because this implementation request did not separately approve the exact fixture plan required by Section 16. Pure tests cover child/team/source/unavailable identity, stable multi-day grouping, empty grouping, and the mixed-timezone edge case. Therefore the implementation is **ready after populated-state UAT**, not yet complete under the prompt's three-way verdict.
- Verification actually run: eight focused weekend/weekend-plan tests, the full 70-test Corralio library/schedule suite, `npx tsc --noEmit -p apps/corralio/tsconfig.json`, `npm run lint --workspace corralio-app`, `npm run build --workspace corralio-app`, and `git diff --check`; all passed. No canonical roadmap or ADR changed because this slice introduced no new accepted product/architecture decision. No migration, RPC, schema, cron, feed fetch, retained-data mutation, external integration, push, or deployment occurred.
- Deferral ledger preserved exactly: Slice 4.2 retains overlap detection, conflict badges/summaries/explanations, family-plan conflict prioritization, and conflict engagement analytics. Slice 4.3 retains household default origin, route-duration lookup/reuse, arrival-buffer policy, leave-by calculation/labels, and route caching/staleness/provider-cost/traffic controls. Slice 4.4 retains explicit or inferred TI association, tournament labels/official links/weather, canonical venue matching, confidence/provenance, and canonical venue writes/evidence. Slice 4.5 retains travel-needed inference, HotelPlanner handoff/search, hotel cards/travel CTAs, commercial attribution/Hotel Program resolution, and booking. Later or separately approved work retains child/team/sport/source/day filters and preferences, manual events, collaboration/invitations/roles/transport coordination, notifications/change briefings, analytics infrastructure, direct provider integrations, remaining disconnect/suppression lifecycle, theme preferences, an interactive marketing demo, and Pro entitlements/pricing/paywalls. None was implemented.

## 2026-08-20 — Slice 4.0C branded product shell (complete locally)

- Audited the pre-slice application before editing: authenticated `/` combined the weekend plan, family CRUD, schedule connection, connected-source health/replacement/assignment, and account actions in one server-rendered page; signed-out/account/recovery surfaces used a reconstructed `C` or text-only name; styles were light-only and largely component-colored; event, family, form, and directions components were already reusable. Repository evidence confirmed Slice 4.0B browser UAT at commit `d51fb8ed` and the complete production brand/font handoff at commit `1f47f1f0`, with no deployable live-text SVG or active port-3002 server.
- Used `docs/brand/BRAND-SPEC.md` as authority. The application now references only outlined production lockups under `apps/corralio/public/brand/`; favicon/PWA assets come from `apps/corralio/public/icons/`, and Open Graph uses `apps/corralio/public/social/corralio-social-avatar.png`. The shipping SVG set remained self-contained and production-ready. `apps/corralio/app/fonts/Manrope[wght].ttf` (Manrope 4.504, weights 200–800, SHA-256 `d0639be45d0af36e798172419d7bd173c4bd4f29e2b76cbb69db1d11bf8b0a40`) is loaded through `next/font/local`; tests and builds make no font download.
- Added a small shared authenticated shell with exactly `This Weekend`, `Upcoming`, and `Family`. Authenticated `/` owns the existing weekend event experience, `/upcoming` is an intentional presentation-only placeholder with no event query, and `/family` exclusively owns children, teams, connection, connected-source health, replacement, sport, and assignment UI. `/family`, `/upcoming`, and `/account` resolve authentication server-side and redirect signed-out requests to `/`; Account and Sign out remain secondary actions. Existing mutations now revalidate both `/` and `/family` without changing their RPC, RLS, ingestion, identity, URL-security, refresh, or replacement semantics.
- Established semantic `background`, `surface`, `surface-subtle`, `foreground`, `muted`, `border`, `primary`, `primary-foreground`, `accent`, `link`, `focus`, `success`, `attention`, and `destructive` tokens with CSS-only system light/dark modes. Brand coral `#CC3F2C` remains the primary control color and passes AA with white text (4.87:1); the darker `#A92F20` hover preserves contrast. On dark surfaces, the approved coral tint `#FF8A73` is also used for focus indication because raw coral is insufficient against navy for that thin non-text indicator. The fixed navy header always uses the approved dark-background lockup; adaptive signed-out/account/recovery surfaces switch outlined variants without client theme detection or flash. Metadata has media-aware theme colors, correct color-scheme, approved favicons/Apple/manifest/social assets, and unchanged `noindex` behavior.
- The responsive consumer shell uses a 1040px content boundary, horizontal thumb-friendly primary navigation, text/weight/underline active indication, 44–48px controls, 3px visible focus rings, labeled forms, reduced-motion handling, and no desktop admin sidebar. The existing child color tokens remain supplementary to text. Event cards received only token/spacing normalization; all rendered information, Friday-through-Sunday window, ordering, assignment/source fallback, sport/icon, location/field, and directions behavior remain unchanged. Deliberate event-card restructuring for family grouping, conflicts, leave-by, tournament context, and contextual travel remains a Slice 4.1+ boundary.
- Browser UAT used local Playwright because the installed browser skill had no `agent-browser` executable. Signed-out `/` and authenticated `/`, `/upcoming`, and `/family` passed at `375×812`, `768×900`, and `1280×900` in both emulated system light and dark modes. All 24 route/width/mode checks rendered expected headings and active states, correct 5.357:1-ratio lockups, local Manrope, 3px focus, no horizontal overflow, error overlay, console error, or page error. Pointer navigation to Upcoming, keyboard activation of Family, Account/Sign-out reachability, Family form labels/controls, and server-side signed-out redirects for `/family`, `/upcoming`, and `/account` passed. Forgot/reset routes were also checked at 375px in both modes with the correct adaptive lockup, and authenticated `/account` retained its branded password surface without product navigation.
- UAT used the existing smoke identity only to establish an authenticated read-only session and found an empty Corralio household projection. No form was submitted after sign-in and no retained household row was mutated. Therefore child/team CRUD, connection, assignment/reassignment/unassignment, replacement, populated health statuses, populated event cards, and directions were intentionally not mutation-tested in this visual slice; their existing rendered controls and source boundaries were inspected, the prior Slice 4.0B browser evidence remains intact, and the applicable offline regression suite passed.
- Automated verification actually run: all 65 Corralio library/schedule tests, focused product-shell tests, `npx tsc --noEmit -p apps/corralio/tsconfig.json`, `npm run lint --workspace corralio-app`, `npm run build --workspace corralio-app`, and `git diff --check`. No migration, database/RPC/cron change, real calendar fetch, production-data mutation, external integration, analytics, theme preference, push, or deployment occurred. Conflicts, leave-by, TI/tournament or venue matching, routing, travel/hotels, collaboration, provider work, and analytics remain deliberately deferred. **Slice 4.0C is complete locally.**

## 2026-08-20 — Brand asset handoff (complete)

- Completed all 10 production SVGs under `apps/corralio/public/brand/`: 4 existing self-contained mark variants plus 6 wordmark/horizontal-lockup variants converted with Inkscape 1.4.4 from the approved Manrope ExtraBold 800 source. Every production SVG parses and contains no live `<text>`, `<tspan>`, font metadata, embedded font, raster data, script, or external dependency.
- Moved the six retained live-text design sources to `docs/brand/source/`. They are intentionally outside Next.js `public`; application code must never reference them. This corrects the earlier `public/brand/source/` staging location, whose contents would have remained deployable even when unreferenced.
- Added the official Google Fonts Manrope 4.504 variable TTF (weights 200–800), OFL 1.1 license, `FONTLOG.txt`, and `METADATA.pb` under `apps/corralio/app/fonts/`. The TTF SHA-256 is `d0639be45d0af36e798172419d7bd173c4bd4f29e2b76cbb69db1d11bf8b0a40`. Slice 4.0C must use this file through `next/font/local`; no Google Fonts build-time fetch is permitted.
- Source and outlined versions produced byte-identical 1200px PNG renders for all six converted assets. Light/dark treatments were visually checked on warm-white/navy at large size and at the 120px minimum lockup width with correct Manrope glyphs, accent colors, spacing, proportions, and no clipping.
- `docs/brand/BRAND-SPEC.md` now records the approved asset-to-background mapping, font provenance/license, production checksums, and validation evidence. The artwork production-readiness blocker for Slice 4.0C is resolved; product-shell implementation and local-font wiring remain deliberately outside this artwork task.

## 2026-08-20 — Slice 4.0B schedule family assignment (complete)

- Added and manually applied `20260820_corralio_slice40b_schedule_assignment.sql` with one `postgres`-owned, locked-search-path `SECURITY DEFINER` RPC. It derives the active owner household from `auth.uid()` before locking the caller-owned connected ICS source, rejects disconnected/foreign sources and inactive or mismatched targets, accepts a child as team-validation context, and persists the established mutually exclusive shapes: both null, child only, or team only.
- Source assignment and every existing `origin_type = 'ics'` event for the exact household/source now update in one transaction. The RPC changes only child/team assignment plus existing trigger-managed timestamps, returns one boolean, and preserves source URL, sport, sync/failure/claim state, stable event IDs, source-event identity, and private event content. No table, column, index, trigger, direct authenticated source-update grant, fetch, cron, reconnect, analytics, or provider work was added.
- Added one authenticated cookie-backed server action and a compact connected-source workflow for explicit unassigned, child-only, and child/team selection. Only active children and the selected child's active teams are selectable. Historical non-null assignments missing from the active projection display `Previous assignment unavailable` and remain reassignable or explicitly unassignable without revealing archival internals.
- Weekend event labels now resolve child-only context directly and team context through team-to-child ownership as `Child · Team`; truly unassigned imported events retain their connected-source label. Names remain relational presentation data and are not copied onto sources or events. The existing weekend feed, locations, directions, source identity, sport, and refresh presentation otherwise remain unchanged.
- Added a read-only catalog audit, a rollback-only synthetic Household A/B behavioral verification, and a separate zero-row cleanup query. The catalog audit passed after correcting an `ORDER BY` expression and confirmed the partial imported-event identity index is scoped to `origin_type = 'ics'`. The behavioral script passed after restoring two omitted anonymous-block dollar-quote terminators; it reached `ROLLBACK` with `auth_users=0`, `households=0`, `children=0`, `teams=0`, `sources=0`, and `events=0`. The separate cleanup query also returned zero for all seven fixture categories, including memberships.
- Offline validation passed all 63 Corralio tests, TypeScript, lint, the production build, and diff checks. Browser UAT then passed at `390px` and `1280px` using an exact synthetic fixture in the dedicated UAT household: initial unassigned fallback, child-only assignment and reload persistence, child-scoped team options, team-only persisted shape, reassignment, explicit unassignment, imported-event propagation, manual-event exclusion, URL secrecy, responsive layout, and absence of console/page/HTTP errors all passed. The unrelated existing source and all 151 of its imported events remained byte-for-byte unchanged in the audited assignment/state fields. Exact fixture cleanup and an independent read-only confirmation restored the UAT household to one source, 151 events, and zero synthetic children or teams. No real feed, browser replacement/refresh, cron, deployment, or push occurred; canonical refresh/replacement assignment preservation remains covered by the passed rollback SQL. Family filtering/grouping, manual-event assignment, imported-event overrides, conflicts, leave-by, routing, TI/venue matching, travel, collaboration, analytics, and provider integrations remain deferred to later slices. **Slice 4.0B is complete.**

## 2026-08-20 — Slice 4.0A.1 broader sports taxonomy (complete)

- Expanded the application-local Corralio planning taxonomy from nine to 17 bounded tokens by adding Tennis, Swimming, Gymnastics, Track & Field, Golf, Wrestling, Cheer, and Dance. Existing sport tokens, labels, and icons remain unchanged; new emoji are presentation-only and are neither persisted nor used for application identity. `other` remains a sports/competitive-athletics fallback rather than free-form calendar input.
- The preflight found no runtime TI sport-taxonomy import or tournament-matching dependency in Corralio. Corralio planning support and future TI enrichment eligibility remain separate capabilities; this slice adds no TI/RI code or matching behavior.
- Preserved the current household model and ingestion boundary. A schedule may remain unassigned, and the existing schema already supports child or team assignment without requiring a fake team. Imported events continue deriving sport through `schedule_source_id`; no Activity entity, event sport column, schedule assignment, or manual-event sport model was added.
- Applied the forward migration `20260820_corralio_slice40a1_broader_sports_taxonomy.sql`. Its preflight found zero incompatible team and schedule-source sport values. The catalog audit confirmed the exact 17-token enforcement, existing function signatures, `postgres` ownership, `SECURITY DEFINER`, locked search paths, intended execution grants, RLS, and authenticated denial of `source_url`.
- The rollback-only behavioral verification passed expanded and existing source sports, bounded `other`, arbitrary-sport rejection, Tennis team storage, unassigned and direct-child source behavior, and cross-household mutation denial. The separate cleanup query confirmed zero synthetic auth users, households, children, teams, sources, and events.
- Updated the canonical product roadmap, architecture decisions, and founder/mentor handoff with ADR-029 and the explicit sports-first/TI-optional boundary. Conflicts, leave-by, tournament matching, weather, travel, manual events, and source assignment remain future sport-agnostic capabilities rather than additions to this slice.
- Offline validation passed all 53 Corralio tests, TypeScript, lint, the production build, and diff checks. No retained private-data mutation, live feed request, push, or deployment was performed. **Slice 4.0A.1 is complete.**

## 2026-08-20 — Slice 4.0A children and teams foundation (complete)

- Activated the existing private `corralio_children` and `corralio_teams` model in the authenticated Corralio home page. Household owners can add and rename active children, add child-owned private teams, and edit team names and optional sport. Archived rows remain excluded; delete, archive, restore, reorder, color editing, team-child reassignment, roster, and collaboration behavior remain deliberately deferred.
- Kept the applied Slice 2 cardinality unchanged: one child may own multiple private team rows, while each private team belongs to exactly one child. Siblings may use separate team rows with the same display name; no shared-team junction, canonical sports entity, or name-uniqueness rule was added.
- Child colors are assigned automatically in the existing six-token order (`forest`, `ocean`, `amber`, `violet`, `rose`, `teal`). The persisted `rose` compatibility key is retained with the accessible fuchsia presentation; no color picker or new schema field was added.
- All family mutations use the authenticated cookie-backed Supabase server client and the existing idempotent owner-household RPC, validate IDs/names/sport server-side, scope writes to the resulting active owner household, and continue relying on the existing RLS and composite household foreign keys. No family mutation uses the service role or accepts a browser-supplied household ID.
- Applied the preflight-protected migration `20260820_corralio_slice40a_family_foundation.sql`, narrowing the existing optional `corralio_teams.sport` field to the established nine-value Corralio taxonomy without coercing existing values. The read-only catalog audit confirmed the intended authenticated-only SELECT/INSERT/UPDATE policies, RLS, composite household FK, exact sport constraint, and absence of DELETE access. The corrected rollback-only behavioral verification passed child/team creation and editing, same-named sibling team rows, invalid-sport rejection, cross-household isolation, and household-reassignment denial. Cleanup confirmed zero synthetic auth users, households, children, and teams.
- Schedule behavior is unchanged. Slice 4.0A does not assign schedule sources or events, fetch/refresh a feed, alter canonical ingestion, mutate imported events, add analytics, or change the weekend plan. Child/team assignment remains Slice 4.0B.
- Offline validation passed all 47 Corralio tests, TypeScript, lint, production build, and diff checks. No retained private data mutation, live feed request, cron invocation, push, or deployment was performed. **Slice 4.0A is complete.**

## 2026-08-19 — Slice 3.3 persistent refresh failure and recovery (local implementation)

- Extended the existing Slice 3.2 source state instead of adding a new `sync_status`: the unapplied migration `20260819_corralio_slice33_persistent_refresh_recovery.sql` adds a private consecutive-failure counter saturated at the fixed threshold of three and a safe `refresh_paused_at` marker. One and two accepted claimed failures remain eligible after the normal 23-hour window; the third keeps the source connected as `error`, preserves its URL/events/household/sport, releases the claim, and excludes it from later cron batches. No table, queue, cron, poller, history rows, or index were added.
- Failure recording remains service-role-only and atomic in `corralio_fail_claimed_ics_refresh_v1`. Only the eight established bounded categories can increment; a stale, expired, duplicate, or mismatched claim returns false and cannot mutate state. The worker now distinguishes such a skipped finalization from an accepted persisted failure while continuing later sources.
- Canonical `corralio_persist_ics_ingestion_v1` remains the single successful-ingestion boundary and resets count, pause, and `last_refresh_error_code` for scheduled, valid-empty, repeat-import, and validated-replacement success. Paused sources cannot be recovered through the ordinary connect form; they use the existing validated replacement flow. Successful replacement updates `last_refresh_attempted_at` in the same transaction, while failed replacement rolls URL, events, freshness, and failure state back together.
- The connected-source UI reads only the household-authorized safe pause timestamp. Below-threshold errors show `Refresh delayed` with automatic-retry copy; threshold sources show `Schedule needs attention`, confirm existing events remain available, and reuse `Replace calendar link`. Authenticated clients still cannot read the exact failure counter, claim metadata, or private `source_url`.
- Added read-only catalog and rollback-only behavioral verification scripts. The behavioral fixture uses full synthetic batches and deterministic low UUIDs, asserts every claim remains inside the synthetic household, and never assumes production has no other eligible sources. The migration was manually applied. The consolidated catalog audit passed all 13 schema, RLS, ownership, locked-search-path, function-execution, and column-privilege checks. The complete rollback-only behavioral verification passed and returned synthetic cleanup counts `households=0`, `sources=0`, and `events=0`; a separate read-only check confirmed the existing UAT family remained intact with one household, one source, and 151 events. Offline validation passed 40 focused Corralio/shared schedule tests, TypeScript, lint, production build, and diff checks; no live feed, production cron, push, or deployment was invoked.
- Deferred unchanged: disconnect/suppression, children/teams/assignment, conflicts, routing/leave-by, collaboration, TI/venue matching, geocoding, notifications, analytics infrastructure, direct provider integrations, and broad schedule-source lifecycle work.

## 2026-08-19 — Slice 3.2 secure scheduled ICS refresh (local implementation)

- Added one daily Corralio Vercel cron at `17 11 * * *` (11:17 UTC) targeting the Node-only `/api/cron/schedule-refresh` route. The route requires the exact `Authorization: Bearer ${CRON_SECRET}` contract, returns `Cache-Control: no-store`, creates only the trusted service-role Supabase client, and returns/logs bounded aggregate results without source URLs or provider errors.
- The worker claims at most 10 active ICS sources per invocation, processes them sequentially, prioritizes never-attempted sources and then the oldest attempt with stable-ID ordering, and uses a 23-hour attempt-based freshness window. `batch_full` is derived only from whether all 10 slots were claimed; no backlog count or unbounded source query was added.
- Added unapplied migration `20260819_corralio_slice32_scheduled_ics_refresh.sql`. It adds nullable attempt/error/claim metadata and three service-role-only, locked-search-path RPCs for deterministic claiming, claimed canonical persistence, and bounded failure finalization. Claims expire after 10 minutes; an expired claim is recoverable even though its attempt timestamp is recent. URL replacement invalidates an active claim so an old feed cannot persist after its secret URL changes. Authenticated users may read only the safe attempt/error fields and still cannot read claim tokens, claim timestamps, or `source_url`.
- Refresh reuses the shared SSRF-safe fetcher and normalization engine, then delegates upsert and explicit-cancellation behavior to `corralio_persist_ics_ingestion_v1`; no parallel event persistence was introduced. A successfully parsed empty feed is sent through that canonical boundary with an empty event list and only explicit cancellation IDs, so unrelated existing events are preserved. Fetch, validation, normalization, event-limit, and persistence failures preserve the prior URL/events, store only a bounded failure code, release the claim where possible, and do not block later sources.
- Persistent-failure auto-disable is deliberately deferred to Slice 3.3 when no existing policy applies. The source remains `error` and becomes eligible again after the next 23-hour window. Disconnect UI, suppression, assignments, conflicts, routing, geocoding, venue matching, collaboration, analytics, and direct sports-platform integrations remain out of scope.
- Added read-only catalog verification and rollback-only behavioral verification SQL. The migration was subsequently applied manually and its catalog privilege contract passed. The first behavioral-verification run rolled back after its fixture incorrectly assumed there were no other eligible production sources; the corrected fixture fills both claim batches with deterministically ordered synthetic rows and asserts that concurrent batches do not overlap or touch existing production sources. The corrected complete verification passed and reached `ROLLBACK`; final cleanup counts were `households=0`, `sources=0`, and `events=0`. No real calendar feed or production cron route was invoked. Validation passed 28 focused/shared schedule tests, Corralio TypeScript, lint, production build, and diff checks. No new table or index was added.

## 2026-08-19 — Slice 3.1.1 password authentication and recovery (local implementation)

- Added password-first returning-user sign-in through the existing Supabase browser client while preserving the existing Magic Link account-creation/fallback path. Invalid credentials—including unknown, passwordless, unconfirmed, and wrong-password cases—share one non-enumerating response.
- Added a small authenticated `/account` password surface. It updates the existing shared Supabase identity through `updateUser`, stores no Corralio password data, does not touch household/RLS state, and explains that the credential follows the same identity into TI/RI where those products support password login. Structured weak-password and stale-session failures map to safe application copy rather than raw provider errors.
- Added `/account/forgot-password`, `/account/reset-password`, and a server-only recovery request route. Recovery uses validated `CORRALIO_SITE_URL` infrastructure configuration to construct `/auth/confirm?brand=corralio&flow=recovery`; it never derives the origin from the request host, a forwarded host, browser input, or `NEXT_PUBLIC_*`, and it fails closed when configuration is absent or invalid.
- Extended the existing callback without changing email/Magic Link behavior: `email` and `magiclink` still return to `/`, while `recovery` or the trusted PKCE flow marker establishes the session and returns a relative 303 to `/account/reset-password`. Relative redirects retain the earlier `0.0.0.0` regression protection.
- Added a manual shared Supabase Recovery template reference with exact Corralio sentinel matching, `.ConfirmationURL` fallback, `type=recovery`, and unchanged non-Corralio fallback behavior. RI's separate server-generated recovery email remains untouched. Remote Supabase templates, redirect allowlists, Auth policies, Vercel variables, deployment, and production data were not changed.
- Validation passed: 21 focused Corralio tests, TypeScript, lint, production build, `git diff --check`, and local HTTP rendering for `/`, `/account/forgot-password`, and `/account/reset-password`.
- Local browser UAT subsequently passed the complete mobile and desktop journey: generic invalid-password handling, Magic Link regression, authenticated password setup and update, password sign-in, forgot-password request, Corralio-branded recovery email, recovery callback, mismatch validation, password reset, single-use-token enforcement, and sign-in with the replacement password. Every successful auth path remained on `localhost:3002`; no `0.0.0.0`, TI, or RI redirect returned.
- UAT confirmed identity/data continuity across Magic Link, password setup, password login, recovery, and reset: the existing household, connected schedule, and imported event remained unchanged, with no duplicate onboarding state.
- The two pre-restart recovery `503` responses were caused by the local `CORRALIO_SITE_URL` value not having been saved into the environment loaded by the running process. The route's `503` is the intentional fail-closed response for missing/invalid trusted infrastructure configuration and does not disclose account existence. Recovery passed after saving the variable and restarting. No code correction was required.
- The reported invalid-password/calendar `503` pattern is not emitted by a shared Corralio validation boundary: password errors come from the Supabase browser request and schedule actions return structured safe errors. The dev-console source-read warning referenced the tracked, present `SignInForm.tsx` and is treated as a non-blocking stale Next.js dev source-map artifact. No production defect remains open from this UAT.
- Production still requires `CORRALIO_SITE_URL=https://corralio.com` in the Corralio Vercel project, the reviewed shared Supabase Recovery template, and the production callback allowlist. Use only the designated Corralio UAT identity for future credential tests; similarly named TI/RI identities are separate auth rows.
- No schema migration, service-role password path, password signup, cross-domain SSO, account merge, or Slice 3.2 refresh behavior was added.

## 2026-08-19 — Slice 3.1 complete

- **Slice 3.1 is complete.** The production-only migration `20260818_corralio_slice31_secure_schedule_connections.sql` was manually applied. Read-only catalog checks confirmed the sport column/constraint, V2 creation and sport-update functions, service-role-only atomic replacement, authenticated sport visibility, authenticated `source_url` denial, and removal of authenticated access to the legacy direct-replacement RPC.
- The complete rollback-only verification passed without assertions. It covered source-level sport, household isolation, successful canonical persistence delegation, failed-replacement URL rollback, cross-household denial, and function/column privileges; the final synthetic cleanup counts were `auth_users=0`, `households=0`, `sources=0`, and `events=0`.
- Final local browser UAT passed at 375×812, 390×844, and 430×932. Corralio-branded Magic Link sign-in returned to `localhost` with an authenticated session in two consecutive sign-out/sign-in cycles; connected status, URL secrecy, persisted Soccer context and event icon, blocked-URL replacement preservation, navigation chooser, copy-address behavior, external Google Maps handoff, touch targets, and overflow all passed with no console errors or failed browser requests.
- Successful replacement with the private UAT feed and a location-less event were not exercised in browser data. Successful atomic replacement/rollback is covered by focused application tests and the production rollback-only SQL; location-less navigation is conditionally omitted by the implemented render path. The isolated reported 503 transport observation is not emitted explicitly by Corralio and did not produce a reproducible functional failure, so no additional defect is open.
- Automatic ICS refresh remains deliberately deferred. **Slice 3.2 is reserved for secure scheduled refresh** with bounded batches, overlap control, per-source failure isolation, and reuse of the existing SSRF-safe fetch and canonical persistence boundaries.

## 2026-08-18 — Auth callback same-origin redirect repair (local implementation)

- Local browser UAT proved the OTP request and shared Supabase Magic Link template correctly carried the Corralio callback, but a dev server started with `--hostname 0.0.0.0` caused the callback route's `new URL(..., request.url)` redirects to emit `http://0.0.0.0:3002/`. The auth cookie was attached to the callback response for the browser-facing localhost origin, then became unavailable after the cross-origin redirect.
- Corralio auth-result redirects now use bounded relative `Location` headers for success, invalid, unavailable, and expired outcomes. The browser therefore remains on the exact origin that received the callback—localhost, a controlled LAN hostname, preview, or production—without trusting `Host`/forwarded-host headers or adding environment-specific origins.
- Focused regression coverage proves all result locations remain relative, never contain `0.0.0.0` or another host, and retain auth cookies on the same 303 response. No Supabase template, allowlist, database, authorization, or email/password behavior changed.

## 2026-08-18 — Slice 3.1 secure schedule connections (local implementation)

- Added optional source-level sport metadata on `corralio_schedule_sources`; sport remains outside the shared schedule engine and imported event rows derive it through `schedule_source_id` rather than duplicating it.
- The connect form now strongly prompts with a native sport selector and clears all submitted fields after success, so the bearer-like ICS URL no longer remains visible. Connected-source cards show only safe name, sport, and status metadata and permit later sport editing.
- Calendar-link replacement uses a fresh empty URL input. The trusted server fetches and normalizes the candidate first, then a service-role-only database function atomically replaces the secret URL and delegates event upsert/explicit-cancellation behavior to `corralio_persist_ics_ingestion_v1`. Failed validation or persistence leaves the previous URL, source state, and existing events intact. The legacy authenticated direct-replacement grant is removed by the migration.
- For this pilot, initial and replacement feeds must currently produce at least one usable event. Existing events absent from a later feed are not deleted; only explicit cancellation identities use the existing deletion behavior. Stale-event lifecycle remains deferred.
- Weekend event cards resolve the source sport for a compact icon. Authorized location text opens a small user-initiated chooser for Apple Maps, Google Maps, Waze, or copying the address. Corralio performs no geocoding, coordinate lookup, routing, traffic calculation, installed-app detection, or preferred-map persistence.
- Added unapplied migration `20260818_corralio_slice31_secure_schedule_connections.sql` and rollback-only verification `scripts/analysis/corralio_slice31_secure_schedule_connections_verification.sql`. Production application and verification remain manual.
- Automatic ICS refresh is not part of Slice 3.1. A future Slice 3.2 should add a bounded protected scheduled server job, overlap control, per-source failure isolation, and reuse of the same SSRF-safe fetch and canonical persistence boundaries. Until then, imports occur at connection or explicit link replacement.

## 2026-08-18 — Shared Supabase auth-email branding contract (local implementation)

- Corralio's single `signInWithOtp` flow now supplies `/auth/confirm?brand=corralio`, allowing the shared Supabase Confirm Signup and Magic Link templates to identify Corralio by exact callback equality without trusting branding data for authorization.
- Added a pure callback builder and focused coverage for localhost and production origins. The application supplies no token hash, authorization code, entitlement, or other credential in `RedirectTo`; Supabase adds the token only in the email template.
- The Corralio confirm handler requires no behavior change: it continues to verify supported code or token-hash callbacks and safely ignores the presentation-only `brand` parameter.
- Supabase dashboard work remains manual. Both shared templates must retain `.ConfirmationURL` when `RedirectTo` is absent and use the guarded `&token_hash=...&type=email` construction when it is present. No remote template, allowlist, email, push, or deployment change was made locally.

## 2026-08-18 — Slice 3 connect schedule → This Weekend (local implementation)

- Added the first product loop: an authenticated household owner can paste an ICS/iCal subscription URL, import normalized events, and see the applicable Friday-through-Sunday events in a mobile-first **This Weekend** view.
- Kept the architecture boundary explicit:
  - the shared `packages/lib/sports-schedule` engine owns SSRF-safe fetch, ICS parsing, recurrence expansion, normalization, stable source-event identity, location/field extraction, and note sanitization;
  - the Corralio adapter owns authentication, owner-household resolution, private source reuse/creation, and translation into Corralio event columns;
  - the service-role-only database RPC owns atomic imported-event upsert, explicit cancellation deletion, and source sync status.
- Calendar URLs remain bearer-like secrets. They are accepted only by server actions/RPCs, matched only in trusted service-role code, omitted from ordinary reads, and excluded from application logs, analytics, user-visible errors, and RPC results.
- The browser receives only RLS-authorized household metadata/events. It applies TI's existing local Friday-through-exclusive-Monday definition to a narrow server-loaded candidate window, avoiding timezone onboarding in this slice.
- No analytics vendor or new analytics persistence was added because the Corralio app does not yet have an analytics abstraction.
- Added focused tests for shared-engine ingestion, household-scoped adapter input, repeat-import identity, raw-location preservation, unauthenticated denial, credential-safe errors, and weekend range filtering.
- Added `20260818_corralio_slice3_ics_persistence.sql`, which is intentionally **not applied automatically**. It must be applied in a controlled production migration before live imports are tested, then checked with `scripts/analysis/corralio_slice3_ingestion_post_migration_verification.sql`.
- Required Corralio runtime variables are `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, and `SUPABASE_SERVICE_ROLE_KEY`. The browser derives the auth callback from the current Corralio origin; that origin must be allowlisted in Supabase Auth. Secrets remain uncommitted.
- Deferred: refresh scheduling, disconnect UI, imported-event suppression, child/team onboarding and assignment, collaboration, analytics infrastructure, conflict detection, leave-by, routing, TI matching, and canonical venue work.

## 2026-08-18 — Slice 2 household and schedule data foundation (applied and verified)

- Applied the reviewed Slice 2 migration to the production Supabase project for private, household-owned Corralio data:
  - households and V1 owner memberships
  - children and teams
  - sensitive ICS schedule sources
  - manual and imported schedule events
- Authorization is household-membership based. Anonymous access is denied, authenticated access is row-scoped with RLS, and composite foreign keys prevent cross-household child, team, or source references.
- Household creation is limited to the idempotent `corralio_ensure_owner_household` RPC. It derives the user from `auth.uid()`, uses a transaction advisory lock, accepts no caller-supplied user ID, and runs as `SECURITY DEFINER` with a locked `search_path`.
- Calendar source URLs are treated as bearer-like secrets:
  - authenticated clients have no `SELECT` privilege on `corralio_schedule_sources.source_url`;
  - authenticated reads must request the explicit safe metadata columns;
  - schedule-source creation and replacement URL writes use narrowly granted RPCs that return no URL;
  - authenticated clients receive no direct schedule-source `INSERT` or `UPDATE` privilege, avoiding constraint-error paths that could expose the protected column;
  - future refresh workers may read URLs only through trusted service-role access;
  - URLs must not appear in logs, analytics, error payloads, or ordinary API responses.
- Authenticated clients may read imported events but cannot create, update, or delete them. Manual events remain household-owner writable.
- Imported-event suppression is deliberately deferred until a refresh flow and user-facing imported-event hide/delete behavior exist.
- Full account and household deletion semantics are deliberately deferred. The membership FK follows the repository's existing `auth.users on delete cascade` convention, but no cross-schema trigger deletes an ownerless household or its private records. Ownerless cleanup requires a separately reviewed design before production scale.
- Completed the controlled, rollback-only production verification covering anonymous denial, RPC idempotency, household isolation, explicit child/team/event household-reassignment denial, composite-FK isolation, membership-write denial, source URL column protection, cross-household source mutation denial, trusted URL access, positive manual-event mutation, imported-event immutability, explicit Team B/Event B isolation, and the currently intentional ownerless-household FK behavior.
- Confirmed the verification transaction rolled back completely: synthetic user, household, child, team, and event counts were all zero afterward.
- Completed the read-only production catalog audit:
  - all six Corralio private tables have RLS enabled;
  - all 15 policies are scoped to `authenticated`, with no `anon` or `PUBLIC` policy;
  - authenticated schedule-source reads expose only the ten approved metadata columns and exclude `source_url`;
  - authenticated table-level schedule-source access is `DELETE` only, with creation and URL replacement confined to the reviewed RPCs;
  - the three authenticated RPCs are `postgres`-owned `SECURITY DEFINER` functions with locked `search_path`, denied to `anon`, and executable by `authenticated` and `service_role`;
  - the `postgres`-owned timestamp trigger helper is not `SECURITY DEFINER` and is not executable by `anon` or `authenticated`.

## 2026-08-17 — Initial application shell

- Established Corralio as the independent npm workspace `corralio-app` under `apps/corralio`.
- Added a static, mobile-first, `noindex` landing shell using Next.js 14, React 18, TypeScript, and app-local styling.
- Added minimal manifest metadata without icons, a service worker, offline caching, install prompts, or push behavior.
- Intentionally added no Supabase, authentication, analytics, database, HotelPlanner, data-fetching, or environment-variable requirements.
- Reserved local port 3002 so RefereeInsights and TournamentInsights can continue using ports 3000 and 3001.
- Vercel project creation, deployment, `corralio.com` attachment, and DNS configuration remain manual and were not performed.
- Validation completed from the monorepo root:
  - npm workspace discovery recognizes `corralio-app`.
  - `npm run lint --workspace corralio-app` passes.
  - `npx tsc -p apps/corralio/tsconfig.json --noEmit` passes.
  - `npm run build --workspace corralio-app` passes and emits the landing page and manifest as static routes.
- The root package lock changed only to register the new workspace and its app-local development dependencies.
# Slice 4.5 — Overture venue corroboration and Nearby foundation (2026-08-25)

- After the user applied the base migration and activation-completeness repair, the corrected read-only catalog verifier passed, followed by the corrected rollback-only behavioral verifier with the exact sentinel `SLICE 4.5 BEHAVIORAL VERIFICATION PASSED; ROLLBACK CLEANUP ZERO`. This confirms the applied table/RLS/function boundary, typed evidence and atomic provenance, idempotency, candidate activation, failed-refresh preservation, Foursquare rejection, and transaction cleanup. Remaining Stage 2 work is the bounded non-private Overture dry-run/UAT, final threshold decision, aggregate quality report, usage check, and independent cleanup-zero confirmation.
- The first rollback-only behavioral run raised `atomic activation failed` because its assertion combined the mutating activation RPC and the subsequent `NOT EXISTS` check in one Boolean expression, whose evaluation order PostgreSQL does not guarantee. The verifier now assigns the RPC result first and checks active state in a separate statement. This was verifier-only; the failed transaction retained no fixture, and no additional schema repair is required.
- After the base migration was applied, the first catalog-verifier run exposed a verifier-only PostgreSQL catalog error: `pg_tables` has `rowsecurity` but not `forcerowsecurity`. The repaired verifier now checks `pg_class.relrowsecurity` and `relforcerowsecurity`, proves that all five tables exist, and includes the failure RPC and activation-completeness contract. The same audit corrected the rollback verifier's duplicate provenance insert/idempotency comparison and added a forward-only activation repair so applied databases reject candidate activation when provenance is missing or a venue/category exceeds its stored cap. Run `20260825_corralio_slice45_activation_completeness_fix.sql` before rerunning both verifiers. No fixture or partial verifier mutation was retained; the failed catalog verifier was read-only.
- Patched the canonical Slice 4.5 prompt with the 11 authoritative specification corrections before implementation: private household origins are categorically excluded; canonical coordinates stay behind a narrow service-only boundary; a bounded no-write Stage 1 sample is permitted; feature ID/GERS status, release/version, existence confidence/match outcome, and property-scoped provenance are separated; Foursquare is excluded absent a human-approved compliance plan; 4.6 feasibility is removed; lifecycle resolution is exact; venue relationships use exactly-one integrity; canonical UAT fixtures are prohibited; and refreshes are hard-bounded, dry-run capable, and atomic.
- Audit confirmed 4.4C is complete and applied, `venues_public` intentionally exposes no coordinates, the evidence table remains ICS-only, and no general enrichment resolver or Overture dependency exists. Current official Overture documentation confirms feature-ID conditional GERS semantics, existence-only confidence, the August 2026 taxonomy transition, and multiple property-scoped sources. The audit also clarified that Food/Coffee filtering applies to the Nearby pool, while a tightly bounded venue-like lookup is inherently required for sports-venue corroboration and does not create another stored Nearby category. These findings match the patched prompt and present no material blocker.
- Added the unapplied `20260825_corralio_slice45_overture_nearby_foundation.sql` migration: typed Overture evidence details, normalized provenance, explicit canonical/provisional candidate relationships, service-only coordinate/lifecycle RPCs, atomic evidence+provenance writing, refresh scopes, bounded refresh metadata, staged candidates, atomic activation, failure preservation, forced RLS, and narrow grants. It does not alter `venues_public` or write `public.venues`.
- Added pure selection/matching/provenance logic, a trusted service adapter, a dry-run-default operations CLI, read-only catalog and rollback-only behavioral verifiers, and the required availability metrics in the existing venue-quality report. Private-origin fields are absent from the Overture runtime boundary. Provisional defaults are a 3-mile radius, 0.70 existence floor, and 15 stored candidates/category/venue.
- No live Stage 1 sample was run: DuckDB/another audited bounded extractor was not present, so the prompt-authorized safe choice is to leave cap/floor provisional until Stage 2. Usage remained zero Overture, Geocodio, OpenRouteService, Mapbox, database writes, migration applications, canonical fixtures/writes, pushes, deployments, and crons.
- Validation passed: 133 Corralio library tests (11 new Slice 4.5 tests), Corralio and ops/report TypeScript, zero-warning Corralio lint, `git diff --check`, and all four production builds (`corp-app`, `corralio-app`, `referee-app`, `ti-web`). Existing unrelated TI/RI build warnings remain. Full audit and Stage 2 commands are in `docs/reports/corralio-slice45-stage1-audit-2026-08-25.md`. Verdict: **SLICE 4.5 STAGE 1 COMPLETE LOCALLY; STAGE 2 REQUIRED**.
